#+TITLE: Representax Roadmap
#+STARTUP: overview
#+TODO: TODO NEXT WAITING | DONE CANCELED
#+OPTIONS: toc:nil num:nil

This file is the canonical Representax roadmap and shared source of truth.
Keep decisions and work items here; record evidence in commits, tests, and
benchmark artifacts rather than duplicating the roadmap elsewhere.

* Papers
** Representax paper [0/17] [0%]
  This section is the finite acceptance program for the Representax
  systems whitepaper. Establish numerical and scientific equivalence first,
  then demonstrate statistically supported systems improvements on shared
  workloads and capabilities enabled by native JAX execution. Sentence
  Transformers 5.4 and later support text, image, audio, and video, so those are
  shared comparisons rather than absence claims. The differentiating claims are
  JEPA-style learning, integrated evaluation/checkpoint-safe arbitrary sharding,
  exact GradCache, and a broader native representation-learning task surface.
  Semantics-preserving automatic execution planning belongs to the separate
  Profilax paper below. Keep exactly one =NEXT= item across the repository.
  Arbitrary sharding/FSDP, mixed precision, unified AnyModality execution, the
  multimodal family matrix, late interaction, cross/reranking, reward, LeJEPA,
  distributed evaluation, and W&B reporting are implemented. Sparse retrieval
  is deliberately deferred. Freeze only the finite experimental panel, then run
  the long paired evidence campaign; do not pull general development into it.

*** NEXT Define measurable monotonic-improvement gates over existing trainers :research:
   Pin the initial model, tokenizer/processor, raw examples and order, objective,
   optimizer and schedule, precision, global batch, update count, and evaluation
   contract. Require numerical parity and non-inferior held-out quality before a
   systems claim. Then report synchronized sustained throughput, accelerator
   utilization and starvation, the feasible-workload frontier, and time to a
   declared quality target over paired repetitions. Report compilation and
   lifecycle latency separately; allocator reservation matters only when it
   reduces feasible work or exposes avoidable materialization.
*** TODO Complete the canonical representation evaluators :research:evaluation:
   Finish =PairClassificationEvaluator= for embedding-distance decisions,
   =ClassificationProbeEvaluator= for frozen-embedding linear probes,
   =ClusteringEvaluator= for unsupervised geometry, and
   =JEPARepresentationEvaluator= for transfer quality and collapse diagnostics.
   Pin preprocessing, probe or clustering algorithms, selection splits,
   hyperparameter grids, seeds, distributed reductions, and primary metrics.
   Admit datasets to the paper panel only after the evaluator agrees with a
   maintained reference implementation on identical embeddings and examples.
**** DONE Implement the four native evaluator contracts
     Pair decisions perform threshold selection over declared similarities;
     frozen probes select logistic regularization on validation data; clustering
     pins MiniBatchKMeans; JEPA representation evaluation combines the same
     linear probe with k-NN and collapse diagnostics. Accelerator work ends at
     frozen embedding extraction and deterministic host reducers own fitting.
**** DONE Match generic reducers to maintained implementations
     Pair decisions match Sentence Transformers 5.6.1 on identical embeddings;
     probe and clustering reducers match their pinned scikit-learn algorithms.
**** TODO Accept the canonical datasets through original-source adapters
     Run Sprint Duplicate Questions, Banking77.v2, Twenty Newsgroups.v2, and the
     selected JEPA transfer dataset without copied intermediate datasets, then
     record identical-example reference parity and distributed reductions.
*** TODO Freeze the experimental workload and model panel :research:models:
   Use two or three representative models per measured track where maintained
   checkpoints and oracles permit: dense text, image-text, audio/video-text,
   late interaction, reranking, reward modeling, and LeJEPA. Keep this measured
   panel distinct from the broader 25-model compatibility table. Admit a new
   architecture only when it fills a missing scientific control, modality, scale,
   or important real-world workload; novelty alone is not a paper requirement.
*** TODO Publish the verified 25-model compatibility table :research:models:
   Select 25 relevant checkpoints across the native families and four
   modalities. Treat this as an evidence audit, not 25 bespoke implementations:
   every listed checkpoint must pass processor, forward, update, export, and
   systems gates before it appears in the table.
*** TODO Measure padding waste before deciding on packing :research:benchmark:data:
   On the frozen pilot workloads, record valid versus padded tokens or media
   elements, bucket occupancy, data-wait time, and accelerator utilization.
   Implement packing for the paper only if padding materially constrains
   throughput and packing preserves identical attention and loss semantics.
*** TODO Benchmark quality, throughput, feasible workload, utilization, and model coverage :research:benchmark:
*** TODO Benchmark compile time separately from steady-state step time :research:benchmark:
*** TODO Benchmark direct MNR and GradCache at equal scientific semantics :research:benchmark:
   Include an explicit distributed-direct-MNR contract: either lower the full
   global relation without constructing an invalid repeated =data= sharding, or
   reject that execution choice at configuration time and direct users to exact
   global-negative GradCache. Do not let a low-level =P("data", "data")= layout
   error define the public capability boundary.
*** TODO Benchmark text retrieval against Sentence Transformers :research:benchmark:
*** TODO Benchmark multimodal retrieval including audio and video :research:benchmark:
   Treat text-image, audio-text, and video-text training as shared workloads
   against Sentence Transformers rather than claiming that upstream cannot train
   those modalities. Compare identical decoded examples and model-ready shapes.
*** TODO Benchmark late interaction and reranking against maintained systems oracles :research:benchmark:
   Include exact flat indexing and bounded streaming retrieval with stable
   document IDs. Report retrieval quality, index build time and size, query
   throughput, memory, and pure-JAX MaxSim performance.
*** TODO Benchmark reward modeling and JEPA workloads against their maintained systems oracles :research:benchmark:
*** TODO Benchmark bounded streaming evaluation during training and checkpoint overlap :research:benchmark:
   Evaluate a configured validation objective and reusable semantic-similarity,
   query/corpus/qrels retrieval, reranking, and task-specific protocols both
   during training and offline. Shard examples or corpus chunks over the named
   data mesh axes, reduce metrics exactly, overlap bounded evaluation with
   asynchronous checkpointing where safe, and prove that neither path starves
   training or changes best-model selection.
*** TODO Measure single-device and single-host multi-device scaling :research:benchmark:
   Cover replicated DDP, exact global-negative GradCache, FSDP, and at least one
   hybrid data/model plan. Measure larger sequences and batches plus a model
   whose replicated optimizer state does not fit; record OOM boundaries,
   feasible work, tokens/s, live memory, and collectives. Physical multi-host
   and TPU claims remain deferred until the hardware is available.
*** TODO Publish reproducible configs, manifests, tests, and benchmark artifacts :research:reproducibility:
*** TODO Write the paper on the monotonic JAX representation-training system :research:paper:
*** TODO Release the Representax systems paper as an arXiv preprint :research:paper:

** Profilax paper: =So you like to go fast?= [0/15] [0%]
  Profilax is a separate installable JAX profiling and semantics-preserving
  execution-planning project. Its paper must demonstrate portability beyond
  Representax. Build and validate the independent core first; only then propose
  focused upstream integrations to maintained systems such as MaxText rather
  than asking those projects to absorb an unproven abstraction.

*** TODO Create =profilax= as a separate installable JAX project :profilax:
*** TODO Profile arbitrary compiled JAX programs through a small adapter :profilax:
*** TODO Inventory devices, topology, memory, and compiler versions :profilax:
*** TODO Search only caller-declared execution parameters under immutable scientific constraints :profilax:
*** TODO Measure compilation separately from warmup and steady-state execution :profilax:
*** TODO Reject plans that alter the scientific projection or violate numerical tolerances :profilax:
*** TODO Report structured infeasibility when the workload requires more accelerators :profilax:
*** TODO Select Pareto-optimal plans for throughput, memory, and compile cost :profilax:
*** TODO Cache plans by program, shapes, hardware, and JAX/XLA versions :profilax:
*** TODO Guarantee feasibility for accepted plans under a fingerprinted environment :profilax:correctness:
   Combine analytic tensor/liveness estimates with measured compiled-program
   profiles and a configurable safety margin. Guarantee only the admitted
   compiled workload under that exact fingerprint; external storage, decoding,
   drivers, and runtime faults remain outside the contract.
*** TODO Integrate Profilax with Representax through =ExecutionPlanner= :profilax:integration:
*** TODO Demonstrate a second independent downstream JAX integration :profilax:integration:
*** TODO Prepare and submit focused upstream PRs after independent validation :profilax:integration:
   Start with maintained projects whose owners accept the use case, such as
   MaxText, and keep each PR small: profiling adapter, plan import, or telemetry
   rather than coupling the downstream trainer to Profilax internals.
*** TODO Benchmark Profilax across multiple downstream JAX programs :profilax:benchmark:
*** TODO Write and release =So you like to go fast?= :profilax:research:paper:

* Initial Representax roadmap [0/42] [0%]
** TODO Grow verified native model families from representation-learning needs :models:hf:
  Keep the pinned Transformers 5.3.0 catalogue as a versioned compatibility
  index and code-generation input, not as a promise to backfill every
  =AutoModel=. Add a native family when a representation task, modality, model,
  or research result requires it, and claim support only after the complete
  numerical, gradient, export, and systems gates pass.
  The first acceptance panel spans BERT-family embedders such as MiniLM or BGE,
  MPNet, ModernBERT/ModernVBERT, SigLIP2, and Jina v5 Omni. Grow beyond it
  organically through multilingual XLM-R-style encoders, modern decoder
  embedders, audio and video towers, state-space models, and mixture-of-experts
  only when they create a concrete capability or scientific comparison.
  Jina v5 Omni's CC-BY-NC-4.0 weights remain an external compatibility oracle;
  native code support must not imply that Representax redistributes those
  weights or grants commercial rights to them.
*** TODO Build reusable native transformer and modality components :models:
**** TODO Generalize positional, residual, MLP, and layer-stack policies through another family
    Extract a policy only when at least two native consumers demonstrate the
    same semantics; do not force superficially similar model graphs together.
*** TODO Port encoder-only text families :models:text:
**** TODO Implement and accept BERT decoder cache and cross-attention modes :models:bert:
*** TODO Port decoder-only, state-space, and mixture-of-experts text families :models:text:
*** TODO Port encoder-decoder text families :models:text:
*** TODO Port vision and image-representation families :models:vision:
*** TODO Port audio and speech representation families :models:audio:
*** TODO Port video representation families :models:video:
*** TODO Port multimodal and composite families :models:multimodal:
*** TODO Port remaining time-series, graph, table, and specialist families :models:
** TODO Unify native and Hugging Face loading without duplicate weights :models:hf:export:
  Expose one public model loader that detects a Representax bundle, local Hugging
  Face directory, or Hub reference. Reuse standard =config.json=,
  Safetensors, tokenizer, and processor assets, while keeping Representax-only
  pooling, route, prompt, precision, and task metadata in a small manifest.
  Construct a typed weightless Equinox template from architecture metadata and
  materialize the selected native weights exactly once; never reread a source
  checkpoint merely to obtain tree structure before deserializing an export.
  Memory mapping or lazy Safetensors reads may bound host memory, but the loader
  must explicitly place every executable JAX array in its destination sharding.
** TODO Export trained models back to Hugging Face-compatible artifacts :models:hf:
** TODO Test tokenizer and processor parity with upstream Transformers :models:testing:
** TODO Test layer-level and full-forward FP32 parity :models:testing:
** TODO Test BF16 parity with explicit numerical tolerances :models:testing:
** TODO Test parameter gradients and objective gradients against upstream :models:testing:
** TODO Test one optimizer update against the upstream reference :models:testing:
** TODO Test safetensor export and upstream reload :models:testing:
** TODO Reduce and amortize ModernVBERT compilation cold-start cost :models:testing:performance:
** TODO Reduce ModernVBERT multimodal-forward allocator and process peak memory :models:testing:performance:
** TODO Revisit a maintained fused-attention kernel after profiling standard JAX :models:testing:performance:pallas:
** TODO Keep small golden tensors in ordinary CI :models:testing:
** TODO Run live upstream parity on a scheduled compatibility lane :models:testing:
** TODO Run accelerator-specific parity for fused kernels separately :models:testing:
** TODO Port a legally clean text-image encoder such as SigLIP2 :models:multimodal:
** TODO Select native audio and video architecture families :models:multimodal:

** TODO Add the host-side =embed= API for preprocessing and batching :api:
** TODO Keep compiled =encode= arrays-in and arrays-out :api:
** TODO Support reusable pooling, normalization, projection, and routing modules :api:
** TODO Reach Sentence Transformers 5.6.1 capability parity without copying its API :api:parity:
  Pin =sentence-transformers==5.6.1= as the repository-only oracle. Maintain a
  reviewed capability ledger rather than equating upstream class names with our
  public API. Dense bi-encoders, late-interaction/multi-vector encoders,
  cross-encoders, and sparse encoders are separate task and systems contracts;
  text, image, audio, video, and composed multimodal inputs cut across them.
  The system-level dense audit and the exact boundary we intend to implement
  live in =docs/dense-system-audit.md=.
*** TODO Complete the dense bi-encoder route :api:models:retrieval:
**** TODO Expand trainer input and integration capabilities
***** TODO Add default, no-duplicate, hashed no-duplicate, and label-group samplers
***** TODO Add proportional/round-robin multi-source and per-source task/loss routing
***** TODO Make prompts and typed routes first-class preprocessing configuration
***** TODO Add source-neutral offline hard-negative mining with artifact provenance
**** TODO Remove duplicate source-checkpoint reconstruction from native reload
     Reconstruct a typed, weightless template from bundled architecture metadata,
     then materialize exported native weights exactly once.
**** TODO Reduce cold train/evaluator compilation and steady-state update time
     Preserve model, task, precision, batch, and validation semantics; report
     persistent-cache behavior separately from an honest cold process.
*** TODO Implement late-interaction and multi-vector representation learning :tasks:late_interaction:
   Support token-level and learned multi-vector embeddings, ColBERT-style MaxSim,
   masking and compression, in-batch negatives, cached/replayed representations,
   retrieval evaluators, and efficient indexing. Treat the released 5.6.1 oracle
   as a baseline inventory, not as the ceiling for this track.
   Use PyLate as the implementation oracle, ColBERTv2 as the scientific oracle,
   and PLAID as the retrieval-system reference. Keep paper measurements in the
   paper campaign above.
** TODO Support =encode_query= and =encode_document= behavior through typed routes :api:retrieval:
** TODO Add Hugging Face Hub loading, local loading, and model export :api:hf:
** TODO Add multi-dataset functionality through source sampling policies :api:data:
** TODO Add unquantized and additional parameter-efficient adapter recipes :api:training:
   Reuse the general model-shaped trainable filter and export contract for
   ordinary LoRA and future useful adapter families; require model, optimizer,
   DDP/FSDP, resume, and export acceptance for each recipe.

** TODO Expand classification beyond paired softmax :tasks:classification:
  Add single-example and multi-label heads, class weighting, evaluator metrics,
  and source mappings without coupling them to the paired SoftmaxLoss contract.
** TODO Flesh out post-resolver data execution slices :data:planning:
  Define implementation contracts and acceptance gates for S3 and object
  stores, image/audio/video decoding and streaming, length- and modality-aware
  bucketing, scientifically valid packing, and deterministic distributed
  iterator resume. Keep JEST as the separate online selection method below.
** TODO Fingerprint artifact revisions, mapping code, recipes, and Git state :data:reproducibility:
** TODO Persist sampling policies instead of materialized mixture datasets :data:reproducibility:
** TODO Support static and scheduled mixture weights :data:sampling:
** TODO Support deterministic shuffling, sharding, and iterator resume :data:runtime:
** TODO Add length and modality-aware bucketing :data:performance:
** TODO Stream text examples from source through the model forward :data:text:
** TODO Stream image examples from source through the model forward :data:image:
** TODO Decode and stream audio efficiently through the model forward :data:audio:
** TODO Decode, sample, and stream video efficiently through the model forward :data:video:
** TODO Train mixed text, image, audio, video, and composed multimodal batches efficiently :data:multimodal:
** TODO Enforce a static model-ready ABI at the compiled step boundary :data:multimodal:runtime:
  Keep =train_step= as the canonical JIT boundary. Resolve variable records,
  decoding, tokenization, sampling, collation, and padding before it, and pass
  only clean tensors in one declared static signature or a small explicitly
  configured set. Compile each allowed signature deliberately before sustained
  training and reject unseen signatures rather than silently compiling inside a
  run. Measure padding waste per signature; do not regain compile stability by
  unconditionally materializing the largest multimodal bucket.

** TODO Optimize compiler-managed FSDP schedules across device counts :runtime:sharding:performance:
   Preserve exact shared-primitive annotations while improving the small-model
   PCIe schedule and eliminating avoidable AllToAlls. Accept only against matched
   numerical trajectories, capacity, and physical throughput; do not reintroduce
   a model-specific hook, separate trainer, custom VJP, or manual collective path.
** TODO Coalesce delayed FP8 scale state and re-run the physical frontier :runtime:precision:performance:
   Replace per-use dynamic weight reductions with persistent delayed scales and
   one coalesced update without changing optimizer or model semantics.  Accept
   only with bounded trajectory error, native optimized HLO, fewer FSDP
   collectives, and a matched end-to-end throughput win.  Evaluate block-scaled
   MXFP8/NVFP4 only on hardware that lowers those formats natively.
** TODO Overlap device placement with Grain input prefetching :runtime:performance:
  First prove from =data_wait_seconds=, placement timing, queue depth, and device
  traces that input or transfer bubbles reduce sustained training throughput.
  Only then build device-ready iterators with non-blocking =jax.device_put= on
  one host and =jax.make_array_from_process_local_data= for distributed input,
  and accept the change only when the observed bottleneck improves.
** TODO Precompile and cache the declared static step signatures :runtime:performance:
  Reuse the exact compiled =train_step= for every batch with the same model-ready
  ABI. Bound any finite signature set in configuration and never let raw record
  variability create an uncontrolled executable population.
* General development [0/17] [0%]

  This section tracks release engineering, optional integrations, exploratory
  research, deferred hardware validation, and work that is useful but is not a
  blocker for the first systems whitepaper.

** TODO Add compressed PLAID/WARP adapters behind the indexing protocol :tasks:late_interaction:
** WAITING Implement sparse encoder training and retrieval :tasks:sparse_encoder:
  Define native sparse representations, pooling, FLOPS regularization, task
  batches, cached losses, retrieval metrics, and one real checkpoint training
  gate. Sparse retrieval is intentionally outside the first paper.
** TODO Add sequence and multimodal packing where scientifically valid :data:performance:
  Proceed only when the paper diagnostic finds material padding waste. Preserve
  segment IDs, reset positions, cross-example attention boundaries, and task
  loss/example semantics; register compatibility per model and task family.

** TODO Cut the next release after dense-system acceptance :release:
  Choose and set the next version only after the dense trainer, evaluator, and
  disk-to-final-model systems gates are complete. Tag only after the exact
  release-ready commit passes public CI and its wheel passes fresh CPU and CUDA
  13 smoke installs through the protected PyPI environment; do not reuse or
  replace the published 0.0.1 artifacts.
** TODO Define the stable public API and deprecation policy :release:api:
** TODO Evaluate Haliax for named axes, model composition, and sharding ergonomics :models:distributed:research:
** WAITING Evaluate TorchAX only as an optional compatibility backend :models:research:
** TODO Audit LCO licensing before considering a native port :models:release:
** TODO Resolve local WebDataset artifacts :data:local:
** TODO Resolve S3 and object-store artifacts first-class :data:s3:
** TODO Implement objective-general online joint batch selection from [[https://arxiv.org/abs/2406.17711][JEST]] :data:sampling:runtime:
  Select a training sub-batch from a larger super-batch using interactions
  exposed by the task objective, rather than scoring examples independently.
  Support easy-reference and learner-minus-reference learnability criteria,
  deterministic sequential chunk sampling without replacement, and arbitrary
  task-provided score matrices. Match the paper's reference algorithm exactly
  before adding cached reference scores, approximate scoring, asynchronous
  selection, distributed selection, or modality-specific accelerations.
** WAITING Validate fully distributed multi-host training :distributed:
  This milestone requires at least two physical accelerator hosts with a
  routable coordinator and is intentionally deferred until those resources are
  available.  Single-host replicated data parallelism and exact GradCache are
  already accepted and remain the local numerical oracle.
*** TODO Validate model and optimizer-state sharding across hosts
   Compare complete updates against the replicated oracle, measure collective
   and resharding costs, and verify that parameter, optimizer, and activation
   placement match the declared plan.
*** TODO Validate exact GradCache and global negatives across physical hosts
   Re-run the accepted process-local ModernVBERT contract with positives and
   negatives crossing the host boundary.  Preserve local encoder replay and
   exactly one final parameter-gradient synchronization.
*** TODO Validate multi-host checkpoint, restore, and failure behavior
   Restore into the same and different topologies, publish checkpoints without
   host races, and fail clearly on coordinator, worker, or storage loss.
*** TODO Record multi-host scaling and transport evidence
   Record topology, interconnect, compile time, steady-state throughput, peak
   memory, communication cost, and failure behavior before making a
   fully-distributed support claim.
** TODO Benchmark JAX =Ref= state against functional Equinox/Optax donation :runtime:performance:research:
  Keep the canonical functional update unless measured memory or throughput
  evidence shows that mutable =Ref= state from the JAX Training Cookbook is a
  monotonic improvement without weakening checkpoint or model ergonomics.
** TODO Evaluate Pallas for performance-critical fused kernels :runtime:performance:research:
** TODO Measure GPU and TPU behavior where hardware is available :research:benchmark:

** TODO Use the completed trainer to train a state-of-the-art representation model :research:model:
** TODO Write the subsequent model paper from that training program :research:paper:
