#+TITLE: Representax Roadmap
#+STARTUP: overview
#+TODO: TODO NEXT WAITING | DONE CANCELED
#+OPTIONS: toc:nil num:nil

This file is the canonical Representax roadmap and shared source of truth.
Keep decisions and work items here; record evidence in commits, tests, and
benchmark artifacts rather than duplicating the roadmap elsewhere.

* Papers
** Representax paper [6/10] [60%]
  This section is the finite acceptance program for the Representax
  systems whitepaper. Establish numerical and scientific equivalence first,
  then demonstrate statistically supported systems improvements on shared
  workloads and capabilities enabled by native JAX execution. Sentence
  Transformers 5.4 and later support text, image, audio, and video, so those are
  shared comparisons rather than absence claims. The differentiating claims are
  JEPA-style learning, integrated evaluation/checkpoint-safe arbitrary sharding,
  exact GradCache, and a broader native representation-learning task surface.
  Semantics-preserving automatic execution planning belongs to the separate
  Profilax paper below. Keep exactly one =NEXT= item across the repository.
  Arbitrary sharding/FSDP, mixed precision, unified AnyModality execution, the
  multimodal family matrix, late interaction, cross/reranking, reward, the
  LeJEPA objective, distributed evaluation, W&B reporting, and V-JEPA 2.1 are
  implemented. The canonical LeJEPA image recipe is frozen as a bounded
  ImageNet-1K comparison. V-JEPA 2.1 remains a distinct paper comparison; sparse retrieval is deliberately
  deferred. Freeze only the finite experimental panel, then run the long paired
  evidence campaign; do not pull general development into it.

  Keep each paper experiment under an enumerated repository directory such as
  =experiments/01-dense-model-training/run.py=. That one public launcher owns
  the frozen configuration, native and pinned-reference dispatch, shared
  Representax evaluation, aggregation, and artifact naming while reusing the
  production training and evaluation implementations. Write durable outputs to
  the matching directory under =/raid/representax-paper/=. Keep lifecycle,
  compatibility, and bounded readiness runners under =experiments/preflights/=
  rather than mixing them with serious experiment entrypoints. Keep one shared,
  locked paper environment under =experiments/= and keep machine scheduling in
  a flat shell file beside each numbered experiment's Python launcher.

*** Paper draft
**** 1. Introduction
     Motivate representation-learning research as a systems problem spanning
     text, image, audio and video workloads. State the paper's claim narrowly:
     Representax provides a portable JAX training surface that often improves
     steady-state throughput while preserving the intended optimization work;
     it does not promise a universal speedup.
**** 2. Representax
     Describe the model/processor bundle, task and loss surface, one canonical
     training entrypoint, exact GradCache, static-shape bucketing, prefetch,
     checkpoint/resume/export, shared evaluation, and explicit JAX sharding.
**** 3. Experimental protocol
     Define the paired-work contract, five seeds, complete optimizer-step and
     examples-per-second metrics, separate compilation and lifecycle costs,
     exact source revisions, GPU and TPU environments, and short-trajectory
     numerical checks. Preserve raw logs so alternate summaries remain
     computable after the runs.
**** 4. Framework and accelerator comparison
     Twelve concrete recipes are complete across five seeds, both frameworks,
     six RTX 4090 GPUs and one 16-chip multi-host v5e slice. The old
     late-interaction row remains systems-only because its source-order MS MARCO
     data was severely duplicated; promote it only after the duplicate-free
     correction below. Show per-recipe speedup with uncertainty, GPU-versus-TPU
     speedup, trajectory correlation, and the capability/reproducibility table.
     Report BERT Base, TPU audio and compiled dense retrieval as explicit limits.
**** 5. End-to-end representation learning [18/18] [100%]
     Run three independent Representax seeds (=7, 42, 773=) for each of four
     canonical workloads. These runs establish useful held-out representations
     and finite-budget learning trajectories; Section 4 already supplies the
     matched reference-framework throughput evidence.
     - Dense retrieval: ETTIN Encoder 150M on duplicate-free MS MARCO, evaluated with
       the shared retrieval evaluator on held-out MS MARCO/TREC data.
     - Late interaction: GTE-ModernColBERT on duplicate-free MS MARCO, evaluated
       with one shared NanoMSMARCO indexing and retrieval protocol.
       Completed negative result retained in artifacts/history. User decision
       2026-09-09: omit this from the main positive learning demonstrations and
       stop recipe tuning for this submission. Do not claim all four recipes
       improved or describe the untested explanation as proven.
     - Image-text: CLIP ViT-B/32 on COCO, evaluated on Flickr30k retrieval.
     - Text-to-any-modality: Jina v5 Omni Nano with COCO text-image, AudioCaps
       text-audio, MSR-VTT text-video and MS MARCO text-text data. Run three seeds
       for each of connector-only, connector-plus-LoRA and full fine-tuning:
       nine Experiment 14 jobs. Evaluate each target modality separately. This
       is fine-tuning, not from-scratch pretraining or an any-to-any claim.
       Frame this as evidence that the toolkit supports effective multimodal
       learning, not a new SOTA method. Report media improvements and text
       forgetting explicitly; novel methods and recipe optimization are future
       work. Fixed-budget gains do not establish an asymptotic plateau.
**** 6. Scaling and sharding [0/9]
     Run a canonical BERT-style masked-language-model workload with the 500M
     controlled encoder for seeds =7, 42, 773= at 1, 2 and 4 GPUs. Hold examples,
     non-padding tokens, optimizer updates, masking, precision and global batch
     fixed: this is strong scaling, not weak scaling. Use replicated DDP with
     deferred gradient synchronization at every device count. The accepted
     tuning screen uses length 512 and 131,072 tokens/update; physical batch 8
     was fastest among the fitting tested cases. Freeze independent seeded
     repetitions before promoting the screen to the nine-run paper panel.
     Report steps/s, tokens/s, scaling efficiency, compilation, peak HBM and
     collective placement. Do not infer communication time from HLO counts.

     Emphasize JAX's sharding surface in the main text, not only the appendix.
     Add one bounded, unseeded capacity frontier showing the largest model or
     batch that fails replicated execution but trains with FSDP or a hybrid
     data/model mesh. Keep the nine fixed-work runs as the scaling statistic;
     the capacity probe is a separate feasibility result.
**** 7. Design analysis
     Reuse the accepted GradCache implementation, bucketing-versus-padding,
     prefetch and layer-unroll measurements. Keep only ablations that explain a
     material end-to-end result; place detailed compilation tradeoffs in the
     appendix.
**** 8. Capabilities, limitations and reproducibility
     Publish the model/modality table, GPU and multi-host TPU execution,
     checkpoint/resume/export and evaluation coverage, exact reference commits,
     environment locks, commands and artifact hashes. State unsupported or
     reference-limited paths directly.
**** 9. Related work and conclusion
     Position Representax as a representation-learning toolkit and portable JAX
     systems contribution, not new compiler work. Summarize the measured
     frontier and the scientific work the framework enables.

*** DONE Define measurable systems-comparison gates over existing trainers :research:
   This is a paper-protocol decision, not an implementation backlog. Freeze a
   small set of shared training workloads that execute in Representax and a
   maintained incumbent such as Sentence Transformers or PyLate. Give each
   workload its own experiment section and show that Representax preserves the
   intended learning problem and held-out quality while improving the systems
   frontier. Do not combine unrelated tasks into one opaque average.

**** Intended claim
     Representax often provides favorable training-throughput improvements over
     maintained reference frameworks while preserving the intended scientific
     training trajectory. The paper reports where this holds and where it does
     not; it does not claim a universal speedup. Additional systems evidence may
     include lower steady-state latency, a larger feasible scientific workload,
     or better accelerator feeding/utilization on the same hardware. A capacity
     improvement means using less live memory for the same work or enabling
     larger models, batches or inputs; efficient FSDP and arbitrary sharding are
     first-class capacity evidence. Compilation is disclosed separately and
     excluded from the primary steady-state verdict. The nine shared sections
     compare against the relevant maintained incumbent:
     Sentence Transformers where it implements the workload, PyLate for late
     interaction, and task-specific maintained references for reward modeling
     and V-JEPA rather than forcing every claim through one framework.

**** Fair comparison contract
     For each task, use the same initial model, training and evaluation data,
     objective, declared optimization recipe and hyperparameters, precision,
     global batch, update count, evaluation cadence, seeds and hardware in both
     frameworks. The JAX and PyTorch optimizer implementations, sharding,
     rematerialization, prefetching, compilation and kernels may differ because
     those are the systems being compared. Report every seed's loss trajectory,
     held-out quality, training throughput, step time, memory and time to any
     quality target. Compilation, evaluation and checkpoint overhead remain
     separately visible. No separate numerical-parity or dry-run certification
     is required for the paper comparison.

     Randomize training order independently between seeds, but give both
     frameworks the identical example order within a seed. Draw from the full
     declared training source rather than materializing a fixed subset; a short
     run's exposure is simply global batch times optimizer updates. Use a bounded
     deterministic subset only when accessing the complete source would impose
     a material download or storage cost, and disclose that exception.

     For GPU comparisons, let each framework use its best native batching under
     the same examples, maximum lengths and scientific batch: fixed sequence
     buckets for Representax and dynamic per-batch padding where the reference
     supports it. Record padded positions so different batching efficiency is
     visible. For TPU comparisons, assign both implementations the same small
     static bucket schedule for variable-length text so neither result is
     dominated by accidental shape recompilation. Fixed-size image, audio and
     video recipes do not need sequence bucketing.

**** Evidence policy
     Use each paired seed run as an independent systems timing process: five
     seeds for the bounded framework sweep and three or more for promoted
     full-training results. Record host timings for every step without adding a device
     synchronization to the critical path; use synchronization already required
     by evaluation, checkpointing and run completion to bound completed work.
     Center the systems tables on complete optimizer steps per second and
     training examples per second. These intervals include input wait, placement
     and framework execution. Also report =perf/tokens_per_second= for
     non-padding training tokens and retain input-wait time as a separate
     diagnostic; keep validation quality under =valid/*=.
     Derive warmup exclusions and summaries after collecting the raw trajectory.
     Do not launch duplicate timing-only processes for the primary comparison.
     Short timing-only runs are reserved for 1/2/4-device scaling and capacity
     boundaries that do not warrant full quality trajectories. Retain raw per-run
     values, confidence intervals, negative results, OOMs and no-crossing
     trajectories. Large traces
     and checkpoints live on =/raid=; Git tracks the immutable config, environment
     and device inventory, compact raw metrics, summary, reproduction command and
     scoped verdict. Throughput measured on different hardware or under a
     different scientific objective is contextual evidence, not a paired claim.
     Every result bundle must record the exact Representax and reference Git
     commits, working-tree patches, package lock and versions, dataset revisions
     and content hashes, accelerator topology, complete command and environment
     variables needed to reproduce the run. Carry these values into the paper's
     reproducibility appendix rather than reconstructing them afterward.

**** Shared experiment sections
     - Dense bi-encoder retrieval: MNR and exact GradCache against Sentence
       Transformers, with held-out information retrieval.
     - Semantic similarity training: a shared pairwise similarity/regression or
       ranking objective, with STSBenchmark.v2 and SICK-R evaluation.
     - Pair classification training: shared positive/negative pair supervision,
       with duplicate/paraphrase classification evaluation.
     - Cross-encoder reranking: shared candidate scoring and ranking loss, with
       held-out reranking metrics.
     - Any-way multimodal retrieval: one larger section with independently
       reported image-text, audio-text and video-text subsections.
     - Late interaction: the same contract against PyLate rather than Sentence
       Transformers when PyLate is the maintained task-specific incumbent.
     - Reward modeling: matched pointwise, pairwise, listwise and process-reward
       workloads against maintained reward-modeling references.
     - LeJEPA: ImageNet-1K multi-crop representation learning against the
       =stable-pretraining= training stack, with the paper's exact objective and
       frozen-backbone k-NN and linear probes.
     - V-JEPA: matched video representation learning against the maintained Meta
       reference, with representation quality and collapse diagnostics alongside
       systems evidence.

**** Maintained systems comparables
     Freeze each oracle in an isolated repository-only environment and record its
     release plus exact commit in every benchmark artifact. The singular source
     inventory is =benchmarks/configs/paper-references-v1.json=; each reference
     result points to a content-addressed runtime-provenance record.
     - Sentence Transformers: [[https://github.com/huggingface/sentence-transformers][huggingface/sentence-transformers]], the dense,
       similarity, pair-classification, cross-encoder and multimodal reference.
       The current Representax oracle is =5.6.1= at
       =7d3eb16a65f62045226e08082ade63cbc71c97a4=; evaluate the released =6.0.0=
       before freezing the paper environment rather than silently mixing both.
     - PyLate: [[https://github.com/lightonai/pylate][lightonai/pylate]] =1.6.0= at
       =346ea2b9c273256919809ac35b3a055970780c9c=, the maintained ColBERT-style
       late-interaction training, MaxSim and retrieval reference.
     - TRL: [[https://github.com/huggingface/trl][huggingface/trl]] =1.10.0= at
       =a7be897f5c8d7b52161f9f8a47d8e6242456b898=, the maintained pairwise
       =RewardTrainer= reference. Add separate task-specific references only for
       listwise, pointwise or process objectives that TRL does not implement.
     - V-JEPA 2: [[https://github.com/facebookresearch/vjepa2][facebookresearch/vjepa2]] at commit
       =204698b45b3712590f06245fbfba32d3be539812=, the official PyTorch code for
       V-JEPA 2, V-JEPA 2-AC and V-JEPA 2.1. Pin a released commit if the project
       cuts a version before the campaign.
     - LeJEPA: use [[https://github.com/galilai-group/stable-pretraining][galilai-group/stable-pretraining]] at
       =9aa93f8b6153eebb73f57d4853ccf8a13d848310= as the maintained systems
       stack and [[https://github.com/galilai-group/lejepa][galilai-group/lejepa]] at
       =c293d291ca87cd4fddee9d3fffe4e914c7272052= as the paper-code source.
       The current stable-pretraining helper uses a different loss weighting and
       flattened SIGReg reduction, so the paired runner must state and implement
       the paper equation explicitly in both frameworks.

**** Dense-retrieval data settings
     Do not reduce dense retrieval to one MS MARCO recipe. Freeze this compact
     canonical panel with immutable source revisions and original qrels:
     - MS MARCO Passage train/dev: primary web-search training workload and the
       four-size scaling analysis; report MRR@10 and Recall@k in-domain.
     - Natural Questions train/test: train MPNet on the pinned Sentence Transformers
       question/answer pairs and evaluate against the pinned MTEB NQ corpus, queries
       and qrels; report Recall@10 and Recall@100.
     - MIRACL train/dev: train the multilingual Jina v5 Small text path on the
       pinned Arabic, English, Hindi and Japanese corpora/topics/qrels; publish
       nDCG@10 and Recall@100 for every language rather than only a macro average.
     - BEIR SciFact, NFCorpus, FiQA, ArguAna and HotpotQA test: transfer-only
       evaluation spanning scientific evidence, biomedical, financial, argument
       and multi-hop retrieval; report nDCG@10 plus Recall@k where meaningful.
     Quora or CQADupStack belongs to the pair-classification section rather than
     expanding the dense systems matrix. No transfer dataset may enter training,
     hard-negative mining or model selection, and any inherited base-checkpoint
     exposure must be disclosed rather than called zero-shot.

**** Model evidence strategy
     Use selected interesting public base checkpoints for realistic fine-tuning
     case studies. Separately train one controlled five-size BERT encoder family
     at approximately 30M, 110M, 500M, 1B and 4B parameters under matched recipes
     to demonstrate that the systems advantage persists with scale rather than
     depending on one architecture or parameter count. The original canonical
     BERT sizes are approximately 110M (Base) and 340M (Large); 30M, 500M, 1B and
     4B are controlled study configurations, not claims of canonical released
     checkpoints. Gate the 4B campaign on a 1B pilot establishing useful training
     coverage and feasible throughput on available hardware. Apply the full size
     ladder to dense retrieval, where training embedding models from scratch and
     fitting larger scientific batches are material use cases. Reranking,
     classification and the other sections use representative architectures
     unless their own result motivates a separate scale study.

**** JEPA task-family decision
     Representax exposes LeJEPA and V-JEPA 2.1 as two explicit selectable tasks
     under one JEPA family, not as modes hidden inside one nominal loss.
     - LeJEPA is the generic lean recipe: predictive agreement plus SIGReg, no
       stop-gradient teacher, no EMA target state and no teacher scheduler. It is
       the simple distributed-friendly representation-learning objective already
       implemented in Representax.
     - V-JEPA 2.1 is the canonical video recipe: spatiotemporal tubelet masking,
       a context encoder and masked-token predictor, stop-gradient targets from
       an EMA encoder, dense prediction and intermediate-layer supervision. It
       receives a direct paired comparison against the official Meta code.
     The tasks share ordinary model execution, mixed precision, sharding,
     checkpointing, reporting and JEPA representation evaluators. Reusable
     V-JEPA mechanisms should be general components where their semantics permit:
     masked-view planning, context/target prediction, EMA state, intermediate
     feature losses and modality tokenizers. They remain composition points, not
     claims that applying every V-JEPA mechanism preserves the LeJEPA objective.
     A later SIGReg-plus-video-prediction composition is a new experimental task,
     not reference-equivalent LeJEPA or V-JEPA 2.1.

*** DONE Complete the canonical representation evaluators :research:evaluation:
   Finish =PairClassificationEvaluator= for embedding-distance decisions,
   =ClassificationProbeEvaluator= for frozen-embedding linear probes,
   =ClusteringEvaluator= for unsupervised geometry, and
   =JEPARepresentationEvaluator= for transfer quality and collapse diagnostics.
   Pin preprocessing, probe or clustering algorithms, selection splits,
   hyperparameter grids, seeds, distributed reductions, and primary metrics.
   Admit datasets to the paper panel only after the evaluator agrees with a
   maintained reference implementation on identical embeddings and examples.
**** DONE Implement the four native evaluator contracts
     Pair decisions perform threshold selection over declared similarities;
     frozen probes select logistic regularization on validation data; clustering
     pins MiniBatchKMeans; JEPA representation evaluation combines the same
     linear probe with k-NN and collapse diagnostics. Accelerator work ends at
     frozen embedding extraction and deterministic host reducers own fitting.
**** DONE Match generic reducers to maintained implementations
     Pair decisions match Sentence Transformers 5.6.1 on identical embeddings;
     probe and clustering reducers match their pinned scikit-learn algorithms.
**** DONE Accept the canonical datasets through original-source adapters
     Run Sprint Duplicate Questions, Banking77.v2, Twenty Newsgroups.v2, and the
     selected JEPA transfer dataset without copied intermediate datasets, then
     record identical-example reference parity and distributed reductions.
***** DONE Add immutable original-source recipes and task-native batch adapters
***** DONE Accept Sprint Duplicate Questions against Sentence Transformers
***** DONE Accept Banking77.v2 and Twenty Newsgroups.v2 against scikit-learn
***** DONE Select and accept one canonical JEPA transfer dataset
***** DONE Prove identical metrics over one-, two-, and four-device data meshes
***** DONE Record source revisions, example fingerprints, configs, and evidence
     Compact evidence lives in
     =benchmarks/results/canonical-evaluator-acceptance-20260825/=. Frozen
     reducer parity remains in the repository parity suite; the acceptance
     artifact deliberately uses deterministic test features and makes no model
     quality claim.
*** DONE Implement and accept native V-JEPA 2.1 :research:jepa:models:video:
   Implement the method defined by [[https://arxiv.org/abs/2603.14482][V-JEPA 2.1: Unlocking Dense Features in Video
   Self-Supervised Learning]] and match the official
   [[https://github.com/facebookresearch/vjepa2][facebookresearch/vjepa2]] repository at the paper-frozen commit. Preserve
   the exact scientific program: dense prediction over visible and masked
   tokens, deep self-supervision from declared intermediate encoder layers,
   image/video tokenizers, context encoder, predictor, stop-gradient EMA target
   encoder, masking and loss reductions. Reuse the ordinary Representax model,
   sharding, mixed-precision, checkpoint, reporting, and evaluator seams.
**** DONE Freeze the paper equations, official code paths, configs, and checkpoint
     See =docs/vjepa2-1-reference.md= for the equation-to-code map and frozen
     Meta commit =204698b45b3712590f06245fbfba32d3be539812=.
**** DONE Implement deterministic image/video views, tubelets, masks, and collation
**** DONE Implement encoder, predictor, intermediate features, and EMA target state
**** DONE Implement dense and deep predictive losses with exact valid-token weights
**** DONE Integrate task construction, training, sharding, checkpoint/resume, and export
**** DONE Match official preprocessing, forward tensors, losses, gradients, and EMA updates
**** DONE Verify inference, a multi-step training trajectory, and clean export/reload
**** DONE Accept single-device plus two- and four-device DDP/FSDP execution
**** DONE Accept V-JEPA representation evaluation and record compact reference evidence
     Compact semantic evidence lives in
     =benchmarks/results/vjepa2-1-acceptance-20260825/=. It records official
     image/video tensor parity, a disk-to-model lifecycle, evaluator execution,
     and physical two-/four-device DDP/FSDP trajectory equivalence. Large-model
     quality and systems measurements remain paper-campaign work.
*** DONE Freeze the remaining experiment manifests [9/9] [100%] :research:models:
   The sections, maintained references, semantic evaluation panel and dense data
   panel above are already frozen. Finish only the unresolved manifest cells in
   this dependency order; do not reopen the scientific matrix while implementing
   the runner.
**** DONE Pin every model, processor, dataset and reference revision
     Record immutable Hub or repository revisions, licenses, preprocessing and
     split fingerprints for every row below. Resolve the exact multimodal and
     V-JEPA training slices before any measured campaign begins.
     The checkpoint revisions pin their processor assets. Dataset licenses are
     disclosed explicitly, including unknown, gated and upstream-term cases,
     rather than inferred from mirrors.
**** DONE Materialize the controlled BERT scaling family
     Use approximately 30M, 110M, 500M, 1B and 4B encoder-only configurations
     with one tokenizer, sequence length, objective, optimizer policy and data
     order. Run the full ladder only on MS MARCO dense retrieval, confirm the
     systems result on Natural Questions, and report the frozen BEIR transfer
     panel. The 4B run is admitted only after the 1B pilot is scientifically
     useful and operationally feasible.
     The exact architecture records and analytical parameter-count acceptance
     live in =benchmarks/configs/bert-scaling-v1.json= and are checked without
     allocating the multi-billion-parameter models by
     =scripts/validate_bert_scaling.py=.
**** DONE Freeze realistic text workloads
     - Dense retrieval: =sentence-transformers/all-mpnet-base-v2= as the public
       checkpoint case study against Sentence Transformers.
     - Semantic similarity and pair classification: MPNet as the primary public
       checkpoint, with canonical BERT Base as one architecture control, on the
       already frozen STS, SICK-R and duplicate/paraphrase evaluators.
     - Cross-encoder reranking: the MiniLM/BERT MS MARCO cross-encoder recipe on
       MS MARCO with held-out TREC DL evaluation.
     - Late interaction: GTE-ModernColBERT on MS MARCO against PyLate, followed by
       the frozen transfer evaluation.
     Immutable model, dataset and reference identities live in
     =benchmarks/configs/paper-text-reward-v1.json=.
**** DONE Freeze reward-modeling workloads
     Make pairwise outcome reward modeling the primary paper comparison: a
     Qwen3-0.6B sequence-classification model with one scalar score, chosen and
     rejected UltraFeedback examples, and the Bradley-Terry log-sigmoid objective
     against TRL =RewardTrainer=. Keep pointwise and listwise objectives as
     contract coverage. Use a small Qwen step scorer on Math-Shepherd against TRL
     =PRMTrainer= for the secondary process-reward comparison.
     The frozen manifest uses vanilla =Qwen/Qwen3-0.6B= plus an independently
     initialized scalar sequence-classification head, not the existing
     vocabulary-logit reranker head. Implement and accept that loader before the
     reward comparison.
**** DONE Freeze multimodal retrieval workloads
     Use CLIP ViT-B/32 for the image-text comparison and the existing
     NVOmniEmbed/Qwen2.5-Omni family for audio-text and video-text. Select one
     immutable training corpus and one held-out retrieval corpus per modality.
     The exact COCO/Flickr30k, AudioCaps and MSR-VTT records live in
     =benchmarks/configs/paper-multimodal-jepa-v1.json=.
**** DONE Freeze the V-JEPA 2.1 workload
     Pin one official configuration, checkpoint initialization policy, video
     training slice and representation evaluation protocol against the existing
     Meta commit. Preserve collapse diagnostics and matched EMA/update semantics.
     Use the official V-JEPA 2.1 ViT-B/16 256px/16-frame configuration with
     random initialization on Something-Something v2 and the official frozen
     representation protocol, including gated ImageNet-1K linear probing. This
     is a matched systems campaign, not a claim to reproduce full upstream
     pretraining quality.
**** DONE Freeze fair comparison contracts for every workload
     For each row, pin the same model initialization, training dataset and order,
     task, optimizer, schedule, global batch, update count, evaluation data and
     primary metric in Representax and the maintained reference. Runtime
     implementation details may differ. Compare held-out quality and systems
     measurements from the full paired runs; do not maintain a separate
     acceptance-status inventory.
**** DONE Freeze the campaign matrix
     =benchmarks/configs/paper-campaign-v1.json= pins paired seeds 7/42/773,
     timing from full trajectories, bounded epoch or update budgets for every
     row, quarter-trajectory evaluation, separate lifecycle checkpoint probes,
     final artifact export and Representax as the shared quality evaluator.
     The seed-7 MPNet/MS MARCO row uses 256 updates at global batch 2,048 on one
     GPU per framework, consumes 524,288 unique pairs from the 669,531-row pinned
     shard without repetition, and evaluates at updates 0/64/128/192/256.
**** DONE Freeze the promoted LeJEPA ImageNet-1K comparison
     The manifests pin ViT-L/16, global batch 16 over two GPUs, two global and
     six local views, 10,000 bounded updates, frozen-backbone k-NN and linear
     probes, and stable-pretraining commit
     =9aa93f8b6153eebb73f57d4853ccf8a13d848310=. The upstream 100-epoch recipe
     remains context, not a reproduction claim: the measured batch-16 canary
     projects it to 8,007,300 updates and 14,034.8 logical GPU-hours on this host.
     Three finite updates pass at batches 1, 8 and 16; batch 16 sustains 5.10
     images/s with about 3.97 GB peak HBM per GPU. Compact evidence lives in
     =benchmarks/results/lejepa-vit-l-capacity-20260831/=.
     Official validation provenance is resolved: all 50,000 local images match
     the official devkit filename-to-synset mapping and are byte-identical to
     the canonical validation archive. Reproducible evidence lives in
     =benchmarks/results/imagenet-validation-provenance-20260831/=.
*** DONE Publish the verified 25-model compatibility table :research:models:
   Select 25 relevant checkpoints across the native families and four
   modalities. Treat this as an evidence audit, not 25 bespoke implementations:
   every listed checkpoint must pass processor, forward, update, export, and
   systems gates before it appears in the table.
   The identity-only panel lives in
   =benchmarks/configs/paper-compatibility-v1.json=. The single executable flow
   is =experiments/preflights/compatibility.py=: it performs checkpoint load, update,
   resumable checkpoint restore, configured evaluation, native inference export,
   and reload through =run_job=. The initial discovery evidence is under
   =/raid/representax/paper-compatibility-v1-20260828-v3/=. All 25 pinned rows
   now pass load, update, resumable checkpoint restore, evaluation, native
   inference export, and inference reload. Continuation and repaired-row evidence
   lives under =/raid/representax/paper-compatibility-*-20260828-v[4-7]/=.
   The compact published table is
   =benchmarks/results/paper-compatibility-v1-20260828/README.org=. It is rendered
   from the pinned manifest and points every row to its passing durable evidence.
*** DONE Measure padding waste before deciding on packing :research:benchmark:data:
   Use ordinary length bucketing for paper training; do not implement GradCache
   sequence packing on the paper critical path. On the four-GPU MPNet/MS MARCO
   pilot, bucketing raised mean token utilization from 17.1% to 32.5% and median
   throughput from 251.80 to 323.65 examples/s (1.285x).

   The matched one-GPU direct-MNR diagnostic proves that packing is scientifically
   valid: real-checkpoint embeddings agree within 9.69e-8, and complete MNR loss
   and parameter gradients match with dropout disabled. A compile-stable fixed
   pack improves fixed-256 throughput 1.240x, but ordinary bucketing improves it
   4.668x and is 3.766x faster than packing. Bucketing uses 4,472 mean token slots
   per update versus 8,640 for the smallest fixed packed ABI covering all 64
   batches. A broad adaptive selector was rejected after causing 13 signatures in
   13 updates.

   Packed repeated evaluation is promising (1.790x final evaluator throughput),
   but it is not needed for the first paper. Frozen image, audio, and video
   workloads already normalize to fixed extents; measure modality-internal mask
   waste in their own preflights before revisiting media packing. Large evidence
   lives under
   =/raid/representax/paper-campaign-v1/preflight/packing/20260828/= and compact
   evidence under =benchmarks/results/paper-packing-preflight-20260828/=.
*** NEXT Complete the final paper experiments [25/27] [93%] :research:benchmark:
**** DONE Repeat seed-7 MPNet/MS MARCO with the default reference collator
     The accepted fair-padding run uses Representax length buckets and Sentence
     Transformers' standard dynamic collator. Across the identical warmed
     update window 131--255, Representax sustains 348.31 examples/s versus
     275.61 examples/s (1.264x). Including native compilation and first use,
     training throughput is 275.13 versus 247.63 examples/s (1.111x), with a
     measured compilation break-even at approximately 260 updates.

     Shared Representax evaluation of both exported artifacts reports final
     NanoMSMARCO nDCG@10 of 0.58205 versus 0.57943 (+0.00262), and final train
     losses of 3.97169 versus 3.97004. Both exports reload and evaluate
     successfully. Peak live allocation is 7.519 GiB for JAX versus 7.021 GiB
     allocated and 11.011 GiB reserved for PyTorch. The run captures Representax
     commit =913cc82e2bb131cd6ff19708776190cbf5696aa9= and Sentence Transformers
     commit =7d3eb16a65f62045226e08082ade63cbc71c97a4=. Complete evidence lives under
     =/raid/representax/paper-campaign-v1/runs/dense-realistic/seed-7-dynamic-padding-20260901/=.

     The first detached launch was rejected before its first update because
     source provenance was read at summary time rather than captured at process
     start. Its preserved tree is
     =/raid/representax/paper-campaign-v1/runs/dense-realistic/seed-7-rejected-late-source-capture/=.
     The corrected frozen 256-update pair was launched on GPUs 0 and 1 after
     accepting the startup-capture fix. It captured Representax commit
     =dea91a608e742588de4bd69dbdc49d9a7954ac61= before worker creation.
     Artifacts, raw metrics, logs, exports and shared offline evaluation belong under
     =/raid/representax/paper-campaign-v1/runs/dense-realistic/seed-7/=.
     That earlier fixed-padding run sustains 348.37 examples/s versus 132.37
     for Sentence Transformers (2.632x). It remains a valid controlled systems
     result, but the reference runner forced every training batch to length 256
     instead of using Sentence Transformers' standard dynamic collator.

     In the controlled run, final losses are 3.97107 and 3.97059. Shared
     Representax evaluation reports NanoMSMARCO nDCG@10 of 0.58315 and 0.59008,
     a -0.00693 absolute seed-7 difference. These remain diagnostics rather than
     accepted default-reference paper values.

     The NVML peak was XLA's preallocated pool, not live model memory. A current
     allocator-instrumented replay measures 7.515 GiB peak live JAX allocation,
     versus 7.059 GiB PyTorch allocated and 9.066 GiB reserved. During the final
     quarter of the serious run, Representax averages 99.2% GPU utilization and
     365.9 W. Batch 4,096 improves native throughput only 0.9%; GradCache chunk
     64 improves it only 0.4%. Keep the frozen batch 2,048, chunk 32 operating
     point rather than optimizing for memory occupancy.

     A later data audit found that this runner consumed source triplet rows in
     order after dropping the mined-negative column. The 524,288-row schedule
     contains only 10,696 distinct query-positive pairs; 97.96% of rows are
     duplicates, and each batch of 2,048 averages only 42.76 distinct positives.
     Retain the paired throughput and systems measurements, but do not use this
     run as representation-quality or convergence evidence.
**** DONE Complete the three-seed MPNet/MS MARCO serious systems result
     Seeds 7, 42 and 773 all complete the frozen 256-update exact-GradCache
     pair, export both final artifacts, and pass the shared Representax
     NanoMSMARCO evaluator. Across matched warmed updates, Representax achieves
     a 1.249x mean throughput improvement with a three-run 95% Student-t
     interval of [1.178x, 1.320x]. Including native compilation and first use,
     the mean improvement is 1.106x with interval [1.067x, 1.144x]. Every
     observed systems result favors Representax.

     The mean final nDCG@10 difference is +0.00262 with interval
     [-0.01897, +0.02420], so the implementations react similarly to the same
     inputs. It is not an accepted quality result: duplicate-heavy batches impose
     an average irreducible MNR loss floor of 3.880, and mean NanoMSMARCO nDCG@10
     falls from 0.6373 to 0.5850 in Representax and from 0.6345 to 0.5822 in
     Sentence Transformers. The aggregate records and fingerprints each input
     and its exact source commits. Seed 7 used
     =913cc82e2bb131cd6ff19708776190cbf5696aa9=; seeds 42 and 773 used
     =ec50f99f24906041467928ae781fde175b0d7e0d=. Their only repository
     difference is this roadmap result record.

     The public reproducer is =experiments/01-dense-model-training/run.py=.
     Canonical evidence and =three-seed-summary.json= live under
     =/raid/representax-paper/01-dense-model-training/=. The three completed raw
     trees are linked there without copying their model exports; future runs
     write to that directory directly.
**** DONE Record the tuned MPNet reference controls
     The optimized-control sweep did not reproduce a Representax regression.
     Native MPNet remained stable at 343.20 examples/s in the accepted
     three-seed result and 345.97 examples/s in the tuning run. What changed was
     the Sentence Transformers execution recipe.

     With both frameworks using GradCache chunk 32 and Sentence Transformers
     running eagerly, Representax reaches 345.97 versus 283.13 examples/s
     (1.222x). Tuning only the eager reference to chunk 128 reverses the result:
     Sentence Transformers reaches 400.89 examples/s, or 1.159x the Representax
     rate. Enabling TorchInductor at reference chunk 128 reaches 594.87
     examples/s, or 1.719x the Representax rate. These are 16-update tuning
     measurements, not a completed 30-update or convergence result.

     Therefore the primary default-library result remains a Representax win,
     while the best-eager and TorchInductor controls expose unresolved GradCache
     and kernel-efficiency work. Report all three configurations explicitly;
     do not describe the optimized-reference reversal as a native throughput
     regression. Exact candidates, scientific settings and source revisions are
     recorded in
     =/raid/representax-paper/01.1-optimized-dense-model-training/tuning/summary.json=.

     A follow-up text-only ModernBERT control uses the exact 149M-parameter
     =jhu-clsp/ettin-encoder-150m= checkpoint underlying ModernVBERT. Across
     three 100-update paired runs, Representax reaches 332.27 examples/s versus
     617.49 for Sentence Transformers with TorchInductor, a 0.538x ratio. The
     loss-curve correlations are 0.933, 0.983 and 0.978. Mean final
     NanoMSMARCO nDCG@10 is 0.1052 versus 0.0900, but the duplicate-heavy data
     and short run do not support a quality claim. The public reproducer and
     compact report are in =experiments/09-modernbert-dense-retrieval/=; full
     evidence is under
     =/raid/representax-paper/09-modernbert-dense-retrieval/=.

     A fixed-shape control removes that comparison's padding confound by
     repeating one deterministic all-valid length-128 pair across batch 128.
     The accepted =GradCache(implementation="custom_vjp")= replays encoder
     chunks into one gradient accumulator and preserves the BF16 policy inside
     its custom backward. Four-process mean throughput at chunks 32, 64 and 128
     is 221.83, 221.41 and 260.79 examples/s. The corresponding gains are
     1.585x, 1.505x and 1.872x over eager Sentence Transformers, and 1.415x,
     1.272x and 1.573x over TorchInductor. Chunk 128 uses full layer
     rematerialization and peaks at 4.84 GiB; chunks 32 and 64 use none.

     The work fixed two measured defects: custom differentiation had lost the
     outer BF16 precision context, and the normal ModernBERT scan retained all
     21 layer outputs despite needing only the final one. Exact FP32 optimizer
     and stochastic-replay tests pass, as do BF16, model, and training tests.
     The control therefore shows that the earlier 0.538x variable-length result
     is not an intrinsic ModernBERT encoder or GradCache limit. Evidence is in
     the experiment's =fixed-shape-control/= artifact directory.

     A matched real-data seed then ran 30 BF16 updates with custom VJP,
     rematerialized GradCache, eager Sentence Transformers, and TorchInductor.
     Custom VJP reaches 361.08 examples/s, 1.058x the rematerialized rate, while
     eager and Inductor reach 391.69 and 667.90 examples/s. Its loss trajectory
     remains close to eager (Pearson 0.989, MAE 0.0368). Representax pads 35.5%
     of token positions versus 22.0% for the reference, executing 20.9% more
     positions per update; normalized by padded positions it is 1.115x faster
     than eager. The remaining real-data throughput blocker is therefore static
     padding granularity, while Inductor also retains a 1.53x kernel-rate lead.
     Full evidence is in =real-trajectory-30-step/=.

     A causal padding ablation confirms that diagnosis. Forcing Sentence
     Transformers to the same query-16/document-128 tensors lowers eager
     throughput from 391.69 to 346.35 examples/s; coarse Representax is then
     1.043x faster. Finer Representax buckets lower executed positions per
     example from 144.0 to 129.2 and raise throughput from 361.08 to 393.16
     examples/s, effectively matching native-padding eager at 391.69. Inductor
     remains faster at 667.90 examples/s, so padding explains the eager
     crossover but not its compiler/kernel advantage. The reproducible command
     is =experiments/09-modernbert-dense-retrieval/run.py padding-ablation= and
     evidence is in =padding-ablation-30-step/=.

     A September 4 pipeline pass resolves the eager-reference gap. Input-major
     scanned weights, shared broadcast RoPE tables, query/document GradCache
     chunks 128/64, and full layer unrolling raise the accepted six-bucket run
     to 582.82 warm examples/s. A subsequent equivalent two-signature run after
     simplifying static layer dispatch reaches 618.67 examples/s: 1.579x eager
     Sentence Transformers at 391.69 and 0.926x TorchInductor at 667.90. Full
     unrolling contributes the decisive
     fixed-shape gain, 382.68 to 542.42 examples/s, while lowering allocator-live
     memory from about 12.6 GB to 9.37 GB. Eight-token buckets are rejected:
     they reduce padding but fall to 561.09 examples/s. Compilation remains
     fully recorded but is not a steady-state rejection criterion because it
     amortizes over long jobs. The current Experiment 09 launcher contains the
     accepted recipe; optimization artifacts are under its =optimization/=
     directory. A short-sequence attention shortcut reached 599.14 examples/s
     but was removed because its 2.8% gain did not justify the specialization.
**** DONE Complete semantic-similarity and pair-classification serious results
     The frozen MPNet primary and BERT Base control each complete four data
     presentations for seeds 7, 42 and 773 in both frameworks. All 12 paired
     cells train to their exact step budget, export and reload both artifacts,
     and use Representax as the shared held-out evaluator.

     MPNet improves mean steady-state throughput by 1.122x on STS-B similarity
     training and 1.170x on Sprint pair classification. Mean shared-evaluator
     quality differences are +0.000217 STS-B Spearman and +0.000128 Sprint
     average precision. The BERT controls match quality but expose architecture
     work still needed: Representax reaches 0.537x and 0.581x reference
     throughput, with quality differences of +0.000394 and -0.004001.

     The initial parallel launch exposed two acceptance issues. A finite Grain
     source stopped after one presentation; training sources now repeat complete
     batches deterministically while =max_steps= remains authoritative, including
     checkpointed iterator state. Concurrent XLA compilation also depressed the
     MPNet reference timing, so the accepted aggregate uses separate
     framework-only timing waves and retains concurrent measurements only as
     diagnostics. The rejected finite-source artifacts remain under
     =/raid/representax-paper/02-semantic-similarity-pair-classification/rejected-finite-source-20260902-140046/=.

     The public reproducer is
     =experiments/02-semantic-similarity-pair-classification/run.py=. Canonical
     evidence and the compact corrected =three-seed-summary.json= live under
     =/raid/representax-paper/02-semantic-similarity-pair-classification/=.
**** DONE Run the paired dense-retrieval campaign preflight
     Run 64 updates of the frozen MPNet/MS MARCO exact-GradCache workload at
     global batch 2048 and length 256 in Representax and Sentence Transformers
     on the same four RTX 4090s with seed 7. Evaluate before and after training;
     checkpoint and resume at update 32; export and reload the final Representax
     model. Record canonical timing and accelerator metrics under
     =/raid/representax/paper-campaign-v1/preflight/dense-realistic/seed-7/=,
     then derive campaign runtime and storage estimates from those measurements.
     Compact evidence lives in
     =benchmarks/results/paper-dense-preflight-20260827/=. The lifecycle passes
     end to end and short-run quality is comparable. The original systems result
     was invalidated because DDP omitted GradCache's data-axis activation
     annotations. The complete corrected paired trajectory reaches 243.26
     examples/s versus 152.80 examples/s for Sentence Transformers, a 1.592x
     throughput advantage. The bounded full trajectory is now frozen at 256
     updates; measure allocator-live JAX memory before making a memory claim.
     The final one-GPU launch canary at global batch 2,048 and GradCache chunk
     32 passes under
     =/raid/representax/paper-campaign-v1/preflight/dense-realistic-single-gpu-capacity-20260831/=.
     Its only warmed Representax interval is 342.51 examples/s versus 130.70 for
     Sentence Transformers, a 2.621x readiness ratio. Two bucket signatures
     compile in 81.8 and 119.6 seconds; retain those cold costs separately from
     the serious trajectory. The eight-update export smoke under
     =/raid/representax/paper-campaign-v1/preflight/shared-offline-pair-smoke-v3-20260831/=
     also proves that the shared Representax evaluator reloads both final
     artifacts and emits identical metric names.

     This preflight shares the duplicate-heavy ordered MS MARCO materialization
     used by the serious dense run. Its lifecycle and same-input systems results
     remain valid; its loss and quality movement are not scientific training
     evidence.
**** DONE Add opt-in timing and accelerator telemetry to the canonical job log
     =logging.timing= records startup, every completed training step, non-padding
     text-token and example throughput, evaluation phases, and export through the
     existing JSONL and downstream reporters. =logging.accelerator= samples every
     visible NVIDIA device in a background thread into the same metric stream.
     Neither option changes the scientific configuration fingerprint, and timing
     does not synchronize the training thread beyond existing lifecycle barriers.
**** DONE Preflight direct MNR against Sentence Transformers
     The fixed-256 allocator regression was =cudaMallocAsync=, not the MNR
     implementation: BFC with a 90% preallocated pool restores Representax from
     48.6 to 148.2 examples/s at the same fixed shape. Converting MPNet's scanned
     layer weights to the established compute-native layout first raised the
     matched fixed-256 result to 151.8 examples/s. Fully unrolling the twelve
     native layers at compile time and storing Q/K/V as one compute projection
     resolves that remaining lowering gap: the corrected trailing-half result is
     223.13 examples/s versus 175.76 for Sentence Transformers, a 1.269x warmed
     speedup. Final losses are 2.88076 and 2.88216. The larger exposed graph
     compiles in 129.94 seconds and crosses the reference's lower cold-start cost
     after approximately 3,362 updates; report that cost separately and use the
     persistent compilation cache for repeated shapes. An exact fresh-process
     cache replay compiled and first-used the step in 6.76 seconds while sustaining
     211.97 examples/s, versus 175.03 examples/s for the freshly rerun reference
     (1.211x). The expensive first compile remains a separately reported cost.

     Pinned Transformers parity passes forward, input-gradient, parameter-gradient,
     AdamW-update, and Hugging Face export/reload gates. The manifest lifecycle
     also passes update, pause, checkpoint resume, configured evaluation, native
     inference export, and inference reload. The diagnostic pre-layout-fix warm
     trace spends 45.8% of GPU time in GEMM/dot and 13.2% in conversions, versus
     63.5% GEMM/dot for the reference, with many smaller kernels around the
     scanned stack and manual dropout-attention path.
     Length bucketing preserves
     the scientific examples and objective while reducing padding, reaching
     495.4 examples/s, or 2.823x the fixed-256 isolated reference. Retain this as
     a controlled padding ablation, not a default-framework claim. Five cold bucket
     compilations cost 72.1 seconds and amortize after approximately 612
     updates under the earlier scanned implementation. Corrected fixed-shape
     evidence lives under
     =/raid/representax/paper-campaign-v1/preflight/direct-mnr/seed-7-fixed256-optimized-v4/=;
     optimized lifecycle evidence lives under
     =/raid/representax/paper-campaign-v1/preflight/direct-mnr/mpnet-optimized-lifecycle-20260828/=.
     A current allocator-instrumented fixed-256 replay confirms 222.15 examples/s
     and 12.004 GiB peak live JAX allocation at direct batch 32, versus 175.76
     examples/s and 11.787 GiB peak allocated for Sentence Transformers: a
     1.264x direct speedup with nearly identical memory. Both systems trained four
     direct updates at batch 64; Sentence Transformers then OOMed during the
     remaining evaluation lifecycle, while all four native rows were first-use
     bucket compilations and therefore supplied no warm summary. Batch 32 remains
     the largest complete matched direct lifecycle point.
     The fair default direct comparison uses native bucketing and Sentence
     Transformers' standard dynamic collator. Across identical warmed updates
     15--24 it reaches 456.99 versus 397.27 examples/s (1.150x), with 7.260 GiB
     peak live JAX allocation versus 5.755 GiB PyTorch allocated and 8.016 GiB
     reserved. The 24-update quality values are only a systems diagnostic.

     The raw MS MARCO triplet source repeats query-positive pairs in source
     order. Treat every direct-MNR number in this section as a systems or
     implementation measurement until duplicate-free batch construction is used.
**** DONE Preflight semantic similarity and pair classification
     Both frozen MPNet workloads now use global batch 256 as eight exact
     batch-32 gradient-accumulation microbatches. Semantic similarity passes
     train, midpoint resume, STS-B/SICK-R evaluation, export, and reload at
     339.1 examples/s; Sentence Transformers reports 206.0 examples/s. Measured
     update 0/4/8 curves stay within 0.0019 Spearman correlation. Pair
     classification passes the same lifecycle at 337.3 examples/s with final
     AP 0.986000, versus reference AP 0.985927. Evidence lives under
     =/raid/representax/paper-campaign-v1/preflight/semantic-pair/semantic-seed-7-v4/=
     and =/raid/representax/paper-campaign-v1/preflight/semantic-pair/pair-seed-7-v3/=.
**** DONE Preflight cross-encoder reranking
     The corrected MiniLM run uses 1,024 balanced MS MARCO query/passage rows,
     eight updates at global batch 128, and deterministic TREC DL 2019
     reranking. Initial nDCG@10 is identical at 0.607210; final nDCG@10 is
     0.605410 for Representax and 0.606599 for Sentence Transformers. Forward
     loss and logits also match in FP32 within 4.77e-7 and 4.77e-6. Checkpoint
     resume, native reload, Hugging Face export/reload, and reference reload all
     pass. The first canary reversed query/document columns in the reference;
     the corrected runner and regression test fix that ordering. The short-run
     timing boundaries differ, so retain the measurements as feasibility
     evidence rather than a paper speedup. Canonical evidence lives under
     =/raid/representax/paper-campaign-v1/preflight/cross-encoder/seed-7-v2/=
     with compact evidence in
     =benchmarks/results/paper-cross-encoder-preflight-20260828/=.
**** DONE Preflight image-text retrieval
     The CLIP ViT-B/32 run uses 512 COCO image/caption pairs, four cached-MNR
     updates at global batch 512, and the full Flickr30k test panel. Final loss
     is 1.677548 for Representax and 1.678729 for Sentence Transformers;
     Flickr30k nDCG@10 moves from 0.737348 to 0.785947 and from 0.736074 to
     0.785997, respectively. Midpoint checkpoint resume, native reload, Hugging
     Face export/reload, and reference checkpoint/export reload all pass exactly.
     The short-run timing boundaries differ, so use them only to establish
     feasibility. Canonical evidence lives under
     =/raid/representax/paper-campaign-v1/preflight/image-text/seed-7-v4/= with
     compact evidence in
     =benchmarks/results/paper-image-text-preflight-20260828/=.
**** DONE Preflight audio-text retrieval
     Single-GPU paired quality, lifecycle, and throughput acceptance passes.
     Matched DDP weak scaling preserves local batch 32 while increasing global
     batch 32 to 64: warmed throughput rises from 2.7334 to 5.2413 examples/s,
     a 1.917x increase and 95.9% efficiency. The audio tower now keeps chunks as
     an independent batch instead of flattening them into one block-masked
     sequence; pinned Hugging Face forward/gradient parity passes. The earlier
     two-GPU FSDP run kept global batch 8 and remains capacity evidence, not a
     scaling measurement. Large evidence is under
     =/raid/representax/paper-audio-text-preflight-b8-20260829/= and
     =/raid/representax/paper-audio-text-scaling-20260831/=; compact evidence is in
     =benchmarks/results/paper-audio-text-preflight-20260829/=.
**** DONE Preflight video-text retrieval
     The pinned LCO-Embedding-Omni-3B/MSR-VTT run passes four finite updates,
     midpoint resume, evaluation, export, and exact inference reload in both
     Representax and Sentence Transformers. Initial video/text embedding cosine
     is 0.99909/0.99985 and warmed throughput is 2.8605 versus 1.4569 examples/s
     (1.963x). The corrected processor preserves sample-row vision tensors and
     the checkpoint's structured system prompt. Compact evidence lives in
     =benchmarks/results/paper-video-text-preflight-20260831/=.
**** DONE Preflight late interaction against PyLate
     The pinned GTE-ModernColBERT/NanoMSMARCO lifecycle passes in Representax and
     PyLate. Under equal static shapes, warmed training is 123.44 versus 53.50
     examples/s (2.31x), encoder throughput is 1,162.05 versus 717.51 documents/s
     (1.62x), final nDCG@10 is 0.71446 versus 0.70901, and Recall@10 is 0.92 for
     both. The rejected 0.225x encoding result used FP32 inference and batch 32;
     the accepted comparison uses the frozen BF16 policy and batch 256. Compact
     evidence lives in
     =benchmarks/results/paper-late-interaction-preflight-20260831/=.
**** DONE Preflight outcome reward modeling against TRL
     The pinned Qwen3-0.6B/UltraFeedback Bradley-Terry run passes four updates,
     midpoint resume, held-out evaluation, native reload, and Hugging Face
     export/reload in Representax and TRL. With equal =[8, 1024]= model inputs,
     warmed throughput is 7.6965 versus 6.6730 pairs/s (1.153x), and final
     held-out pairwise accuracy is 0.625 for both. The rejected dynamic-padding
     comparison processed unequal padded-token counts. Compact evidence lives in
     =benchmarks/results/paper-outcome-reward-preflight-20260831/=.
**** DONE Preflight process reward modeling against TRL
     The pinned Qwen3-0.6B/Math-Shepherd run passes four updates, midpoint
     resume, held-out evaluation, export, and exact reload in Representax and
     TRL. Final step accuracy is 0.8125 versus 0.8047 and warmed throughput is
     52.72 versus 16.18 examples/s (3.258x). A rejected reference wrapper
     multiplied gradients by the 32 accumulation microbatches; the corrected
     wrapper forbids arbitrary loss-normalization arguments. Compact evidence
     lives in =benchmarks/results/paper-process-reward-preflight-20260831/=.
**** DONE Preflight V-JEPA against the official Meta implementation
     The four-update V-JEPA 2.1 ViT-B/16 run matches the official Meta loss
     trajectory within 0.000309, preserves collapse diagnostics, passes midpoint
     resume and exact final reload, and reaches 6.161 versus 1.314 examples/s
     (4.689x). The native task now excludes padded target IDs and matches Meta's
     per-mask reduction and mask-token selection. The bounded preflight mirror
     exposes only =train=, so disjoint train examples serve as a readiness
     holdout. The authoritative Qualcomm SSV2 package, including validation
     videos and labels, is now pinned in the multimodal manifest and remains to
     be materialized for the paper quality run.
     Compact evidence lives in
     =benchmarks/results/paper-vjepa-preflight-20260831/=.
**** DONE Accept Natural Questions and MIRACL real-data lifecycles
     Natural Questions with MPNet and MIRACL Arabic, English, Hindi and Japanese
     with Jina v5 Small each pass two real-data updates, midpoint checkpoint and
     resume, start/end canonical information-retrieval evaluation, final export,
     reload, and exact exported-artifact metric reproduction. The four-query
     subsets prove execution only, not quality. Compact evidence lives in
     =benchmarks/results/paper-nq-miracl-acceptance-20260831/=.
**** DONE Accept the controlled BERT scaling ladder through the 1B gate
     The controlled 30M, 110M, 500M and 1B BERT encoders each pass three real
     MS MARCO MNR updates through =run_job=, checkpoint/resume, configured MS
     MARCO and Natural Questions evaluation, export/reload, exact parameter
     digest reproduction and exact same-topology exported evaluation. Every row
     uses FP32 master parameters with BF16 compute. The 1B row establishes the
     gate for a separate four-GPU 4B capacity canary; no 4B allocation has yet
     occurred. Compact evidence lives in
     =benchmarks/results/bert-scaling-ladder-msmarco-20260831/=.
**** DONE Reject the paired controlled BERT parameter-scaling screen as scientific evidence
     The numbered launcher at =experiments/07-bert-parameter-scaling/run.py=
     initializes both frameworks from the same frozen state and fixes the real
     MS MARCO rows, global batch, asymmetric cosine MNR objective, AdamW policy,
     BF16 compute, and seed. The 1,000-update runs use fixed length 128 for
     updates 1--900 and fixed length 512 for updates 901--1,000. This is
     random-initialized retrieval training with a BERT-style systems schedule,
     not masked-language-model pretraining.

     The data program is invalid for MNR. It takes the first 64,000 rows from a
     triplet file, discards each mined negative, and preserves grouped source
     order. Those rows contain only 1,306 distinct query-positive pairs; 97.96%
     are duplicates. A batch of 64 averages 2.29 distinct positives and has an
     average irreducible duplicate-positive loss floor of 3.509. The 500M and
     1B curves remain at the random-batch value =ln(64) = 4.159=; the 30M model
     primarily learns to distinguish the two or three groups in each batch.
     Therefore no quality, convergence, or model-scaling conclusion is valid.
     Same-row throughput remains a record of executing the named shapes and
     topologies, not an accepted paper comparison.

     The completed rows below are retained as topology-specific observations,
     not as one canonical scaling curve. The topology-selection rule was not
     applied consistently: the 30M and 110M DDP rows were promoted while full
     single-GPU rows also existed and were substantially better for the
     reference framework.

     | Size | Topology | Representax 128 ex/s | Reference 128 ex/s | Ratio | Representax 512 ex/s | Reference 512 ex/s | Ratio |
     |------+----------+-----------------------+--------------------+-------+-----------------------+--------------------+-------|
     | 30M  | 1 GPU      |                263.21 |             413.73 | 0.64x |                105.10 |             300.14 | 0.35x |
     | 30M  | 2-GPU DDP  |                466.22 |             363.37 | 1.28x |                193.94 |             315.53 | 0.61x |
     | 110M | 1 GPU      |                178.29 |             189.71 | 0.94x |                 46.76 |              94.87 | 0.49x |
     | 110M | 2-GPU DDP  |                162.27 |              75.93 | 2.14x |                 69.66 |              66.37 | 1.05x |
     | 500M | 1 GPU      |                 41.38 |              70.37 | 0.59x |                 11.26 |              21.52 | 0.52x |
     | 1B   | 4-GPU FSDP |                  3.16 |               5.98 | 0.53x |                  3.18 |               5.67 | 0.56x |

     Every row fixes global batch 64, so each same-row pair performs the same
     optimizer work on the same device count. That is a valid direct comparison
     for the named topology. It is fixed-work strong scaling, however: local
     batch falls as device count rises. It does not measure weak scaling,
     per-device efficiency, or the benefit of increasing global batch with more
     hardware. Because device count and sharding also change across sizes, raw
     throughput must not be compared across rows as model-size scaling. The
     original selection mixed distributed-capability testing, native throughput
     selection, and memory feasibility; no general framework-speed claim should
     be drawn from the selected subset.

     The 30M, 110M and 500M rows completed paired training, export/reload, and
     shared Representax evaluation. Representax completed the full 1B lifecycle;
     Sentence Transformers completed all 1,000 timed updates, but Accelerate's
     legacy FSDP full-state export deadlocked after training, so no paired 1B
     quality claim is made. The timing trace is complete and retained. A corrected
     all-rank CPU gather and rank-zero export passed an independent two-GPU 30M
     reload canary, and reference timing and loss rows now flush durably.

     The conditional 4B row did not complete one update. Four-GPU FSDP failed
     after rematerialization at a 22.31 GiB executable footprint, and six-GPU
     FSDP still failed a 617.91 MiB allocation while XLA reported forced
     replication around the 39-head attention layout. Further parameter/head
     sharding work is deferred. Durable runs, rejected attempts, source snapshots,
     and logs live beneath
     =/raid/representax-paper/07-bert-parameter-scaling/=.
**** TODO Rerun late interaction with duplicate-free MS MARCO pairs
     Rebuild the late-interaction data with unique queries and positives, then
     rerun the five-seed Representax/PyLate GPU comparison. Treat the retained TPU
     measurement as a same-shape systems diagnostic unless a bounded TPU rerun is
     inexpensive; do not use the old duplicate-heavy loss trajectory as training
     evidence.
**** DONE Run the eighteen end-to-end representation-learning jobs [18/18] [100%]
     Run only Representax for seeds =7, 42, 773= on the four frozen Section 5
     recipes: ETTIN dense retrieval, GTE-ModernColBERT late interaction,
     CLIP ViT-B/32 image-text training, and Jina v5 Omni Nano text-to-any-modality
     fine-tuning with three adaptation strategies. Use a real data presentation or the frozen update
     budget, retain every training metric, evaluate the initial and final model
     with the shared held-out protocol, and save resumable checkpoints at 50%
     and 100%. Intermediate evaluation is optional where it is cheap; learning
     curves are not a primary paper figure. Section 4 already supplies the paired
     reference comparisons; do not launch reference workers here.

***** DONE Train and evaluate dense retrieval for three seeds
      ETTIN Encoder 150M completed =3,817= updates per seed on duplicate-free MS
      MARCO at a mean warm rate of =415.57 examples/s=. Mean nDCG@10 is =0.5794=
      on NanoMSMARCO, =0.5404= on TREC DL 2019 and =0.2922= on Natural Questions.
      The complete summary is
      =/raid/representax-paper/11-dense-retrieval-convergence/summary.json=.
***** DONE Train and evaluate late interaction for three seeds
      GTE-ModernColBERT completed =1,000= updates per seed at a mean warm rate of
      =216.61 examples/s=, with midpoint resume and exact native/Hugging Face
      reload. NanoMSMARCO nDCG@10 fell from =0.7123= to =0.6869= on average, so
      this is a retained negative scientific result: easy random in-batch
      positives do not improve the already-trained checkpoint. The complete
      summary is
      =/raid/representax-paper/12-late-interaction-convergence/summary.json=.
      The approved hard-negative recipe completed seeds =7, 42, 773=, with
      batch =512= and =1,000= updates. Outputs are separate under
      =/raid/representax-paper/12-late-interaction-convergence/hard-negatives/=.
      All three have 1,000 recorded updates with no skipped/nonfinite steps.
      The consistently scored initial nDCG@10 is =0.7103915=; final scores are
      =0.6927199= (7), =0.6913334= (42), =0.6933328= (773), mean =0.6924620=
      with sample SD =0.0010243=. Hard negatives did not reverse the decline:
      retain this as a negative quality result, not positive convergence evidence.
      Read-only follow-up 2026-09-09: all seeds have 999 nonzero updates; the
      initial zero update is expected from the zero-start warmup. Mean loss
      over the first/last 100 updates falls 0.0611/0.0385 (7), 0.0630/0.0404
      (42), 0.0580/0.0360 (773). All seeds lose two net top-1 successes
      (27/50 to 25/50) and one net top-10 success (46/50 to 45/50).
      This is a reproducible small-panel decline, not evidence of a stalled
      optimizer or a measured full-corpus quality collapse.
      The checkpoint was distilled; this run uses hard-label contrastive loss
      with one mined negative and in-batch candidates, without teacher scores.
      Recipe-induced forgetting is a hypothesis, not an isolated cause.
      Temperature 0.02 is explicitly documented by PyLate and is not itself
      a bug. The pinned checkpoint's preprocessing metadata matches the local
      copy; its limits are query 48/document 300, while our train and evaluation
      consistently use 32/256. Evaluation encodes in BF16 and scores in FP32;
      it is not a full-FP32 encoder evaluation. The evaluator saves aggregate
      metrics, not query rankings, so paired per-query uncertainty requires
      re-evaluation. Proposed next action, not launched: score saved initial
      and final models with a consistent precision policy, save per-query
      rankings, and add an untouched held-out retrieval panel before tuning.
      Superseded by user decision 2026-09-09: no additional late-interaction
      evaluation or retraining is required for submission; preserve the negative
      evidence and omit it from the main positive learning demonstrations.
      Do not interpret the small difference from the older =0.7123= initial
      scoring protocol as training progress. The user observed low/jumpy power, utilization
      and memory-bandwidth readings. Afterwards, check larger GradCache replay
      microbatches (currently =8=) while keeping the effective batch and scientific
      settings fixed. Telemetry alone does not establish the bottleneck; compare
      complete warm optimizer-step times before changing the recipe.
***** DONE Train and evaluate image-text retrieval for three seeds
      CLIP ViT-B/32 completed one five-caption COCO presentation, =1,150= updates
      per seed, at =317.22 +/- 2.04 examples/s=. Mean Flickr30k nDCG@10 improved
      from =0.7373= to =0.8148= for text-to-image and from =0.7168= to =0.7665=
      for image-to-text. Every run resumed at 50%, exported both native and
      Hugging Face artifacts and reloaded exactly. The complete summary is
      =/raid/representax-paper/13-image-text-convergence/summary.json=.
***** DONE Freeze the Jina v5 Omni Nano text-to-any-modality configuration
      Native Nano and Small loading, all modality paths, full-precision upstream
      parity, training, resume and export/reload are accepted. The agreed model
      is =jinaai/jina-embeddings-v5-omni-nano-retrieval= at
      =b7287f6b6b562e25bc4a28b939d1f936484b4137=. Run all nine jobs: seeds
      =7, 42, 773= for connector-only, connectors plus LoRA across towers, and
      full fine-tuning. Include text-text alongside text-image, text-audio and
      text-video for the LoRA and full-fine-tuning arms. Connector-only samples
      only the three media-text sources, retaining text-only evaluation.
      The study does not claim training every modality combination.

      Jina's paper, [[https://arxiv.org/html/2605.08384v1#S4][Section 4]], samples one
      source dataset per batch by mixture weight, rather than mixing arbitrary
      modality pairs within a batch. It uses bidirectional InfoNCE, temperature
      =0.02=, Matryoshka dimensions =32,64,128,256,512,768= for Nano, batch
      =256= and =15,000= steps. These are source-recipe facts, not yet our budget.
      Agreed sampling weights are uniform per active source: =1/3= each for
      connector-only media sources, =1/4= each including text-text for LoRA/full.
      These are probabilities per update, not equal compute or exact counts.
      The final configuration below fixes loss, learning rates, input sizes,
      microbatches and budget. A pure text-text batch bypasses trainable connectors, so
      omit it from connector-only training and use text-only evaluation instead.
      Report this training-distribution difference explicitly across strategies;
      do not count a zero-gradient text-only forward as a learning update.

      Dataset inventory at the pinned revisions: COCO2017 has =118,287= training
      images and =5,000= validation images, generally five captions per image.
      OpenSound/AudioCaps has =45,178= training rows, =2,223= validation caption
      rows and =4,411= test caption rows; caption rows are not unique clip counts.
      VLM2Vec/MSR-VTT =train_7k= has =7,010= videos and =140,200= captions;
      its =test_1k= has =1,000= video-caption pairs. The currently pinned MTEB
      MSR-VTT mirror instead exposes =879= test rows. Pick the evaluation
      protocol and disjoint validation IDs explicitly. Prepared duplicate-free
      MS MARCO contains =488,576= query-positive rows; reuse the NanoMSMARCO,
      TREC DL and NQ evaluation paths, with final panel selection still open.
      Existing local audio/video assets are bounded preflight subsets, not the
      full training and evaluation sources for this convergence study.

      Data preparation (2026-09-08):
      - Cache the original pinned AudioCaps Parquets and MSR-VTT videos, not
        expanded NumPy media or the old truncated preflight tables. Verified
        repository sizes: AudioCaps =43.94 GB=, MSR-VTT =2.27 GB=; /raid has
        sufficient space. Downloading the full repository does not add held-out
        rows to training: use only the agreed training split (=train_7k= for
        MSR-VTT), with validation/test sources kept separate.
      - Reproducible command: =experiments/.venv/bin/python
        experiments/14-text-to-any-modality/data.py=. Reuses the shared HF Hub
        cache and the existing revision pins. Background tmux session
        =representax-exp14-download= fetches audio then video; completion
        records (source, revision, snapshot path) and errors go to
        =/raid/representax-paper/14-text-to-any-modality/download.log=.
        Both complete snapshots downloaded successfully.
        Snapshot completion does not yet certify decoding every media file.
      - Decode/resample/tokenize selected examples in bounded prefetched
        batches. Full raw-media caching is deliberate here; do not describe it
        as a demonstrated network-streaming training result. The built-in HF
        resolver uses =streaming=False=. Real-media integration now passes;
        the final input-size and processor configuration remains under review.
      - Use one source per optimizer batch, selected by seeded mixture weights,
        not example-level mixing or fixed round-robin. Library configuration is
        =mix(..., sampling_unit="batch")=. It batches sources separately, repeats
        them independently and checkpoints source cursors plus seeded PCG64 state.
        Batch mode uses one producer and a bounded shared prefetch queue.
        Default example mixing, serialization and fingerprints remain unchanged.
        Removed the experiment-local sampler. Use the uniform weights above.
      - Jina's existing processor handles text/image/audio/video payloads and
        finite shapes. With approval, extended the library RetrievalCollator
        to preserve query/document payloads rather than calling str(); the
        processor owns modality handling and validation. Removed the duplicate
        experiment-local PairedMediaCollator. Text strings remain unchanged;
        callers wanting numeric values converted to text must map them explicitly.
        Library =decode_image=, =decode_audio= and =decode_video= now decode
        original cached media on demand; field mappings remain experiment-local.
        Codec dependencies live in the =media= extra and experiments/uv.lock.
        Shared uv environment synced successfully. Root all-group uv resolution
        still has an unrelated pre-existing Transformers 5.5/5.6 legacy-parity
        conflict; the experiment environment is independently locked and works.
      - Validation: 142 data/collator/experiment CPU tests passed (one deselected),
        plus five selected job/checkpoint/text integration tests. Default mixing,
        weighted sampling, prefetch, iterator/Orbax round-trips and decoder
        policies are covered. Tests do not establish convergence.
      - Real GPU 0 integration: eight finite, non-skipped updates, checkpoint at
        four, resumed to eight. Nonzero update norms at every step. Source order
        =video,text,text,image,audio,text,image,text=. Separate real-processor
        check reproduced the resumed batches exactly. Output directory:
        =/raid/representax-paper/14-text-to-any-modality/integration-checkpoint-fix=.
        This used batch two, BF16 compute/FP32 weights, text-tower LoRA rank four,
        two-second audio, two video frames and 4096-pixel image/video limits.
        These are integration-only settings, not the nine-run recipe.
      - First attempt in =integration= failed saving PCG64's 128-bit state as
        object arrays. RNG state now uses a lossless JSON string in the cursor;
        three real Orbax round-trip cases and the GPU rerun pass. The rerun
        emitted a nonfatal Orbax bool-metadata warning but restored successfully.
      - Reproduce through =experiments/14-text-to-any-modality/run.py= commands
        =prepare=, =inspect=, =check-data= (CPU) and =canary= (GPU). Use the same
        =--output= for preparation and execution. No nine-run training launch yet.
      - GradCache extension (2026-09-08): both replay implementations accept
        Jina's model-owned batch-to-scan layout. The processor supplies a small
        sample-to-visual-group lookup; chunks gather complete merge groups,
        preserve positions/audio layouts, and rebase token/segment indices.
        Partial chunks repeat a real row without allowing its vision patches
        to attend across the repeated copies. No tower equations/weights changed.
      - Custom VJP now uses the same =loss_from_representations= boundary for
        plain MNR and Matryoshka-over-MNR, including weighted/randomly selected
        dimensions. One accumulated representation cotangent, one encoder replay
        per chunk, not one per prefix. NumPy sampling and RNG serialization are
        unchanged. Late interaction still uses rematerialized GradCache; custom
        VJP for token representations and distributed custom VJP remain unsupported.
      - Validation: 61 focused tests passed, one GPU-only test skipped in the CPU
        lane. Includes all four toy Jina modalities, direct/rematerialized/custom
        losses, FP32 gradients and AdamW updates, unequal visual sizes, partial
        chunks, non-row-ordered media, stochastic replay and BF16 precision scope.
        This is not full-model adaptation-strategy/capacity acceptance. An identity
        precision-test fixture had exactly-zero Matryoshka prefixes; the existing
        normalizer has singular derivatives there. Used nonzero prefixes in that
        fixture; normalization behavior was not changed by this extension.
      - Prior text performance control: =benchmarks/grad_cache_regression.py=,
        baseline =7ede45dd5258477bb069ffd07960fbae140cf7fc=. Four-layer random
        ModernBERT, width 256, batch 128, query/document lengths 32/128, FP32
        weights/BF16 compute, chunks 32/64/128, both replay implementations.
        All six StableHLO and optimized HLO programs identical before/after.
        Thirty alternating warm observations each: current/baseline rate
        =0.9968--1.0010=. No measured regression in this bounded control;
        not a new paper throughput or convergence result. GPU scatter reduction
        variability is recorded separately from strict FP32 parity tests.
        Full timings, source snapshots and HLO:
        =/raid/representax-paper/14-text-to-any-modality/grad-cache-regression-unified=.

      - Real-media capacity sweep: =chunk_sweep.py= fixes batch 32, six updates,
        custom VJP and Matryoshka; chunks =1,2,4,8,16,32= for each of text,
        image, audio and video. All 24 cells execute serially on GPU 0, with a
        fresh process per cell. Uses the proposed full input caps below, but
        current text-only LoRA rank 4/alpha 8, not all three final strategies.
        Outputs: =/raid/representax-paper/14-text-to-any-modality/chunk-sweep=.
        Retain complete step intervals, cold first-use time, finite/skipped
        flags, raw logs and process-lifetime JAX live-memory peak. This bounded
        repeated-data probe is not a convergence result.
      - Completed 2026-09-08: 22/24 cells passed six finite/non-skipped updates,
        each with five warm observations; video chunks 16/32 OOM. All use
        =activation_rematerialization="full"= in addition to custom-VJP replay,
        FP32 parameters/BF16 compute, AdamW 1e-5, weight decay 0.01, seed 7.
        Warm pairs/s include input wait, exclude first compilation/execution:
        | Chunk |   Text |  Image | Audio | Video |
        |-------+--------+--------+-------+-------|
        |     1 |  55.60 |  40.77 | 17.91 | 17.69 |
        |     2 |  98.33 |  77.78 | 25.92 | 17.87 |
        |     4 | 168.32 |  97.96 | 32.64 | 17.84 |
        |     8 | 241.62 | 104.22 | 32.30 | 16.88 |
        |    16 | 283.10 | 102.96 | 32.10 |   OOM |
        |    32 | 312.20 | 108.49 | 47.55 |   OOM |
        Peak allocator-live GiB (not the 21.17 GiB reserved BFC pool):
        | Chunk | Text | Image | Audio | Video |
        |-------+------+-------+-------+-------|
        |     1 | 3.78 |  3.88 |  4.81 |  4.21 |
        |     2 | 3.79 |  4.06 |  5.04 |  4.62 |
        |     4 | 3.81 |  4.12 |  5.09 |  5.48 |
        |     8 | 3.84 |  4.33 |  5.16 |  8.93 |
        |    16 | 3.92 |  5.19 |  5.32 |   OOM |
        |    32 | 4.14 |  9.73 |  5.74 |   OOM |
      - Memory inspection: =memory_report.py= summarizes XLA buffer assignments,
        counting aliased buffers once. Retained state is 3.54 GiB; temporaries
        at chunk 32 are 0.60 GiB text, 2.17 GiB audio and 5.06 GiB image.
        Video chunk 16 requires 24.92 GiB temporaries (28.65 GiB total), including
        individual =f32[12,16384,16384]= vision attention values of 12 GiB.
        Video chunk 32 fails autotuning while requesting 48.02 GiB, before a
        final buffer assignment exists. Keep its compiler/OOM logs, not a
        fabricated memory total. Successful cells lacking dumps use a separate
        one-update memory-only replay; original timing observations remain intact.
      - Limits: video has material input waiting; another CPU compilation job
        overlapped according to the user. Rates are bounded capacity-screen
        observations, not isolated paper results or cross-modality efficiency
        comparisons. Different chunks are not strict BF16 trajectory-parity tests.
        Text/image/video repeat 32 distinct real pairs; audio reads an original
        training shard. This sweep did not validate the three final adaptation
        strategies; the separate strategy lifecycle checks are recorded below.
        Candidate follow-up, not yet run: same GradCache with layer checkpointing
        disabled, keeping batch and science fixed. Per-modality chunk selection
        is not yet wired into the mixed-source job (one scalar chunk setting).

      Approved final settings, executable in =config.py=:
      - Nine jobs, unchanged Nano revision and seeds; uniform weights as above.
      - Bidirectional InfoNCE, temperature =0.02=, Matryoshka prefix dimensions
        =32,64,128,256,512,768=. Global batch =32= and chunk =2= for every
        adaptation strategy; all three passed the capacity/lifecycle checks below.
      - Text maximum =1024=, sequence buckets =128,512,1024=; image budget
        =256*256= pixels; video eight uniformly selected frames at =224*224=
        pixels per frame; audio mono 16 kHz, up to ten seconds. These differ
        deliberately from the tiny integration settings above.
      - AdamW, weight decay =0.01=, clip norm =1=, 6% warmup then cosine decay;
        learning rates: connectors =2e-4=, connectors+LoRA =5e-5=,
        full =1e-5=. LoRA rank =8=, alpha =16= across tower linear layers.
      - =2000= updates per seed; evaluations at 0, 25%, 50%, 75% and 100%;
        resumable checkpoints at 50% and 100%, final native inference export.
        Use the existing regular evaluation cadence: omit the proposed 10%
        evaluation rather than changing the core training loop for this recipe.
      - Full-source metadata manifests live in
        =/raid/representax-paper/14-text-to-any-modality/training-data/=.
        COCO reuses Experiment 13's 117,760 prepared images (not all 118,287
        canonical images), with 588,800 pairs; AudioCaps uses all 45,178 available
        training clips; MSR-VTT uses all 7,010 training videos with 129,809
        distinct video/caption pairs, removing repeated captions per video.
        Text reuses each seed's 488,576-query duplicate-free MS MARCO table.
        Seeded metadata ordering prevents within-batch duplicate media/captions;
        source mixing stays uniform and homogeneous at batch level. Grain does
        not reshuffle the already ordered files. Incomplete source tails are
        dropped, sources repeat as needed, and only consumed media are decoded.
      - Held-out panels, no query/corpus truncation: Flickr30k test 5,000 text
        queries / 1,000 images; OpenSound AudioCaps test 4,411 queries / 883 clips;
        VLM2Vec MSR-VTT test_1k 1,000 queries / 1,000 videos; NanoMSMARCO 50 queries
        / 5,043 documents. Audio/video train-test media IDs are disjoint.
        Each panel has an independent native IR accumulator, full 768-dimensional
        embeddings and cosine retrieval, reporting recall@1/5/10 and nDCG@10
        plus the standard IR diagnostics. This is text-to-media evaluation,
        not bidirectional or exhaustive MTEB evaluation.
      - =run.py prepare-training= builds metadata without media copies;
        =run.py inspect-training --strategy connectors --seed 7= prints the
        executable config; =run.py launch --gpus 0 1 2 3= dispatches nine runs.
        Start seed 7 per strategy, allow later seeds after ten updates, use one
        shared compilation cache, and stop pending seeds of an arm if it fails.
        =run.py train --strategy full --seed 7 --resume= resumes explicitly.
        Source hashes, dataset pins, exact config, Git commit, package versions,
        environment-lock hash, metrics and checkpoints remain under =/raid=.
        This change is experiment wiring only; no core model/loss/optimizer code
        was changed. Focused full-source/config/evaluator tests: 25 passed.
      - Do not silently enable GradCache in the serious recipe: the batch-2
        real-media custom-VJP/Matryoshka canary completed eight updates including
        checkpoint/resume at tiny input caps. The batch-32 capacity sweep above
        covers proposed input caps, but only text LoRA. Keep the scientific batch
        unchanged while selecting execution chunk sizes for the final strategies.
      - Independent parameter selection landed in =c52afbe=. LoRA insertion and
        trainability are separate: =training.trainable_pattern= selects floating
        leaves by regex after adapter insertion; omitted settings preserve the
        existing defaults. Connector selection is
        =^\.(vision\.merger|audio\.projection)\.=; connectors+LoRA also selects
        =\.lora_[ab]$=. Full fine-tuning selects all floating parameters.
      - User approved training the active vocabulary delimiter rows as well:
        =training.trainable_embedding_rows={".text.token_embedding": (128257, 128258)}=.
        These are Nano's audio start/end tokens. Media placeholder embeddings
        are replaced by features; the separate loaded audio BOS/EOS parameter
        is unused by this forward path and is not selected as a connector.
        Selected rows are stored independently of a frozen base vocabulary,
        avoiding full-vocabulary gradients/moments and frozen-row weight decay.
        Native inference bundles retain this representation; HF export merges
        the selected rows into the standard embedding table.
      - =strategy_preflight.py= runs the three strategies serially on GPU 0,
        with eight updates, checkpoint after four and resume through eight.
        Batch =32=, chunk =2=, full activation checkpointing, custom-VJP GradCache,
        BF16 compute/FP32 parameters and the Matryoshka loss above. Preflight
        learning rate is =1e-5= for all arms, not the proposed final arm-specific
        rates. Sources are the bounded real-media capacity inputs described
        above; connector-only omits text and the other arms include all four.
        Evidence: =/raid/representax-paper/14-text-to-any-modality/strategy-preflight/=.
        All three passed eight finite/non-skipped updates and checkpoint/resume:
        | Strategy          | Trainable parameters | Peak live GiB |
        |-------------------+----------------------+---------------|
        | Connectors        |           12,787,200 |          5.13 |
        | Connectors + LoRA |           21,675,520 |          5.67 |
        | Full fine-tuning  |          947,615,232 |         18.82 |
        Both partial-training arms changed connectors and selected delimiter
        rows without changing any frozen leaf. The LoRA arm changed leaves in
        text, vision and audio towers. Before/after parameter hashes, exact
        JobConfig, step records, checkpoints and logs are retained per arm.
        Memory is process-lifetime allocator-live peak, including load/compile
        and resume, not reserved BFC memory or a static executable estimate.
        Full fine-tuning fits on one RTX 4090 at these caps/chunks; no FSDP was
        needed. This does not certify larger chunks or unrestricted media.
        These are readiness checks, not convergence or throughput comparisons:
        source coverage is sparse and cold compilation/checkpoint intervals
        are present. These checks preceded the final launch recorded below.
        Orbax emitted an optional progress-metadata bool-dtype warning while
        restoring, and XLA emitted one autotuning timer warning in the full arm;
        both runs continued successfully. Preserve these warnings in the logs.
      - Focused validation: 11 selection/gradient/AdamW tests, one native/HF
        sparse-row export test and 14 experiment-data/config tests passed.
        An existing mixed-precision BERT bundle test fails on the unused pooler
        FP32/BF16 reload template; reproduced with pre-change code and left
        outside this change. No claim that the entire test suite passed.
      - Final dispatch launched from =044f690= on GPUs =0,1,2,3= under user service
        =representax-14-nine-runs=. Seed 7 for connectors, connectors+LoRA and
        full fine-tuning starts on GPUs 0, 1 and 2 respectively; GPU 3 takes a
        warmed follow-on seed after ten updates. At launch verification all
        three workers entered the initial held-out evaluation; no completed
        optimizer updates or final results are claimed yet. Runtime status:
        =/raid/representax-paper/14-text-to-any-modality/runs/dispatch.json=;
        launcher log =/raid/representax-paper/14-text-to-any-modality/launch.log=.
        Verified all 127,211 referenced nonempty media/shard paths exist.
      - Completion reviewed 2026-09-09: all nine runs completed 2,000 updates,
        all five scheduled evaluation passes and final native export. Verified
        all nine export completion markers and checkpoint model/optimizer/progress
        directories at steps 1,000 and 2,000. Across 18,000 updates there were
        no skipped or nonfinite records. Dispatcher has no running/pending jobs;
        service exited successfully. This verifies saved artifacts, not a new
        final-model reload parity experiment. Runs themselves did not resume;
        resume acceptance came from the prior three-strategy preflights.
      - Aggregate source-hashed raw metrics, per-run provenance, all evaluation
        histories and mean/sample-SD statistics are saved in
        =/raid/representax-paper/14-text-to-any-modality/summary.json=.
        Mean final nDCG@10 across three seeds:
        | Strategy          | Flickr30k | AudioCaps | MSR-VTT | NanoMSMARCO |
        |-------------------+-----------+-----------+---------+-------------|
        | Initial, no LoRA  |    0.5724 |    0.4378 |  0.3731 |      0.6509 |
        | Connectors        |    0.7121 |    0.4691 |  0.4772 |      0.6509 |
        | Connectors + LoRA |    0.7466 |    0.4799 |  0.4984 |      0.6121 |
        | Full fine-tuning  |    0.6987 |    0.5028 |  0.4733 |      0.5994 |
        LoRA media baselines differ slightly (0.5723/0.4372/0.3743); saved deltas
        use that arm's own initial values. All three media panels improve in
        every seed. Connector-only preserves the frozen text result exactly;
        LoRA/full lose 0.0387/0.0515 mean text nDCG despite text training.
        LoRA gives the strongest image/video outcome; full gives the strongest
        audio outcome. Late-stage media gains remain, especially for full tuning:
        these are useful fixed-budget learning results, not proof of a plateau.
      - Mean warm complete-step rates: connectors =11.55 +/- 0.06=,
        connectors+LoRA =14.71 +/- 0.30=, full =14.42 +/- 0.18= pairs/s.
        Include input wait; exclude recorded cold first-use steps. Do not compare
        these as equal-work speedups: connector-only has three media sources,
        whereas the other arms include cheaper text batches with probability 1/4.
        The final Section 5 panel is complete (18 runs); the superseded three
        original late-interaction runs are retained separately. Section 6 strong
        scaling and the capacity-frontier probe remain unfinished.
**** TODO Run the nine ModernBERT MLM strong-scaling jobs [0/9] [0%]
     Superseded initial 48-hour proposal (not launched): retain the
     500M model, seeds 7/42/773 and 1/2/4 GPUs; bound each run to 300 updates,
     270 at length 128 and 30 at length 512. Target 32,768 input tokens/update
     (256 or 64 sequences respectively), conditional on single-GPU capacity.
     Freeze token batches, masking, initialization, precision, optimizer and
     layer-rematerialization policy across device counts. Measure phases
     separately, including input wait and retaining cold-start observations.
     The initial readiness audit found no encoder MLM task/head; the addition
     approved on 2026-09-09 is now implemented and tested as described below.
     Clarification 2026-09-09: the native ModernVBERTTextEncoder is already a
     text-only ModernBERT implementation exposing per-token hidden_states.
     Reuse that encoder unchanged with the new ModernBERTMaskedLM head and
     registered masked_language_modeling task. This is not a new transformer.
     A ModernBERT-based run must be labeled as such, not as reproduction of
     the original BERT architecture/recipe. Final model size/configuration is
     not yet approved; retain the nine-cell fixed-work scaling design.
     Allow at most six hours for a separately approved MLM implementation and
     parity/readiness gate, then decide with the user whether to proceed or
     defer it. Do not silently substitute the old random-initialized retrieval
     ladder. Reserve the final 12 hours for analysis and writing, not tuning.
     Capacity proposal: one bounded replicated-versus-four-device-FSDP case,
     with identical model/batch/dtypes; target 4B only if the canary fits.
     Record a real OOM and successful warm updates rather than promise fit.
     Implementation acceptance 2026-09-09:
     - Sparse prediction gathers masked hidden positions before dense/GELU/norm
       and the tied vocabulary decoder. The embedding table occurs once in
       the parameter/optimizer tree. The old encoder and GradCache paths are
       unchanged. No GradCache is needed for additive token cross entropy.
     - FP32 masked-token CE and accuracy ignore -100 labels and invalid rows;
       zero supervised tokens give zero loss/gradients. Gradient accumulation
       weights each microbatch by its supervised-token count, not row count.
     - Host masking uses seeded, fixed-count sampling without replacement,
       ceil(0.15 * eligible_tokens), and 80/10/10 mask/random/unchanged corruption.
       This is explicitly not Bernoulli masking. Special/padding tokens are
       excluded as targets; target slots are padded statically with -100 labels.
     - Tests pass for independent NumPy loss/gradient checks, ignored NaNs,
       sparse/dense parameter-gradient equivalence, Transformers full-FP32
       logits/loss/all-parameter-gradient parity, unequal and empty accumulation
       groups, and two/four-device CPU DDP/FSDP versus one-device updates.
       run_job checkpoint/resume is exactly equal to uninterrupted state;
       native exported-model reload reproduces logits exactly. Focused existing
       registry/dense/ModernVBERT tests also pass; no full repository suite run.
     - GPU 0 synthetic probe: 505,072,128 parameters, batch 8, FP32 parameters,
       BF16 compute, AdamW, full layer rematerialization, full static unroll,
       xla attention, no state donation. Ten warm observations after one first
       execution per shape: length 128 =0.21570 s/update=, =4,747 tokens/s=,
       =80.27 s= compilation; length 512 =0.35095 s/update=, =11,671 tokens/s=,
       =84.97 s= compilation. These repeat device-resident synthetic batches,
       exclude input loading, and are not final accumulated-batch throughput.
       HLO buffer sizes (arguments/output/temp; aliases zero): length 128
       =6,060,875,056 / 6,060,866,118 / 2,638,458,368= bytes; length 512
       =6,060,903,280 / 6,060,866,118 / 8,162,484,736= bytes. Process-lifetime
       live peaks =17,001,409,280= and =22,521,869,312= bytes include setup.
       HLO and per-update values are under
       =/raid/representax-paper/15-modernbert-mlm-scaling/preflight-20260909-mlm-xla/=.
     - Explicit cudnn attention failed before compilation with broadcast mask
       shape [batch,1,1,sequence] (Bias must have same seq length as QKV).
       Retained the existing xla route; no transformer change to work around it.
       The new MLM path is verified; the real corpus/tokenizer, fixed-global-batch
       accumulation and GPU scaling cells still need freezing and execution.
     - Real-text canary approved and started 2026-09-09. Launcher:
       =experiments/15-modernbert-mlm-scaling/canary.py=. This is a bounded
       readiness check, not approval of the final nine scientific runs.
       WikiText-2 raw training revision =b08601e04326c79dfdd32d625aee71d232d685c3=;
       BERT uncased tokenizer revision =86b5e0934494bd15c9632b12f734a8a67f723594=.
       Seed-shuffle the full nonempty training rows, tokenize, separate with SEP,
       and form contiguous blocks. Keep the previous 505,072,128-parameter model
       and its 32,768 vocabulary rows (tokenizer has 30,522 IDs). No pretrained
       weights loaded; this is a ModernBERT architecture with a diagnostic BERT
       tokenizer, not reproduction of the released ModernBERT recipe.
       Six updates per device count: three length-128, three length-512;
       32,768 input tokens/update, local microbatch eight, gradient accumulation,
       BF16 compute/FP32 masters and AdamW, full layer rematerialization,
       donated state. Exact data/masking hashes, source snapshots, compilation,
       complete mask/transfer/update intervals and HLO buffers are retained under
       =/raid/representax-paper/15-modernbert-mlm-scaling/canary-20260909-*=.
       Four small-model GPU DDP/FSDP tests passed. An additional joint FSDP,
       data-sharding and accumulation test passed on two GPUs against the global
       FP32 AdamW update. The four-device case FAILED before execution: explicit
       batch sharding cannot infer reshape [16@data,4] -> [2,8,4]. The shared
       accumulation code reshapes first and supplies split sharding afterward;
       it needs the output sharding at reshape time. Core change awaits user
       approval. Do not declare the four-device scaling gate passed or launch
       the nine-run campaign. Ten non-distributed MLM tests also passed here.
       Full-model one-GPU length-128 passed: compile =159.77 s=, warm complete
       update =1.32684 s=, =24,696 input tokens/s=; loss =10.6231 -> 9.1512=.
       One-GPU length-512 also passed: compile =175.54 s=, warm complete update
       =1.42942 s=, =22,924 input tokens/s=, final loss =8.56406=. Live peak
       =21,329,434,624 bytes= (~19.86 GiB). All six updates finite/non-skipped.
       Only two warm observations per shape: capacity/readiness evidence, not
       a stable paper estimate. Two-GPU FSDP completed all six finite/non-skipped
       updates: length 128 =8.30735 s/update=, =3,944 tokens/s=, compile =116.16 s=;
       length 512 =2.65076 s/update=, =12,362 tokens/s=, compile =126.90 s=.
       Both runs have identical scientific configurations and per-update hashes
       for original tokens, corrupted tokens, positions and labels. Maximum
       absolute loss difference =0.00012684=; final losses =8.5640583= versus
       =8.5639668=. FSDP is slower than one GPU (0.160x / 0.539x speedup), so
       the scaling performance gate is NOT passed. HLO has all-gathers inside
       the accumulation loop; communication is a supported hypothesis, not a
       separately profiled fraction of elapsed time. GPU 0/1 topology is NODE,
       without NVLink. Next proposal: replicated DDP for this fits-on-one-GPU
       model, reserving FSDP for capacity; first approve/fix the four-device
       explicit-reshape issue. Four-GPU full-model launch remains held.
       Summary: =canary-20260909-summary.json=; test failure evidence:
       =canary-20260909-gates.json=; actual installed packages:
       =canary-20260909-environment.txt=, all in the same experiment output root.
       All canary processes exited and GPUs 0-3 were free after completion.
     - Larger scientific-batch follow-up approved 2026-09-09: keep the 505M model,
       seed 7, sequence length 128, optimizer, precision and source unchanged.
       Compare 131,072 tokens/update (1,024 sequences, four times the previous
       batch), with local microbatch 8 versus 32, on one and two GPUs. Each
       device pair has identical global work; changing microbatch only changes
       accumulation. The prior 32,768-token cells remain the smaller-batch
       controls. Four updates per new cell, first execution excluded from warm
       summaries; record all observations. Configurations are the named
       =large-batch-micro8= and =large-batch-micro32= cases in =canary.py=.
       Artifacts use matching case/device names in the experiment-15 output
       root. No core training changes, no four-device reshape workaround, and
       no additional scientific campaign launched. Two configuration/data tests
       pass: selected token budget is consumed and microbatch changes preserve
       exact supervised batches. All four initial cells completed; extended the
       same fixed-global-batch configuration to local microbatch 64 in parallel.
       Also repeated microbatch 32 and 64 on the original GPU 0/1 pair rather
       than confound microbatch changes with GPU 2/3's different PCIe placement.
       Eight runs / 32 optimizer updates completed, all finite and non-skipped.
       No compiled-capacity rejection occurred: microbatch 64 fits both setups.
       Results at length 128, tokens/s (one GPU / two-GPU FSDP on GPU 0/1):
       | Global tokens/update | Local microbatch | One GPU | Two GPUs |
       |----------------------+------------------+---------+----------|
       |               32,768 |                8 |  24,696 |    3,944 |
       |              131,072 |                8 |  25,522 |    3,954 |
       |              131,072 |               32 |  32,127 |   13,501 |
       |              131,072 |               64 |  31,184 |   22,018 |
       The first row is the earlier short canary, not a new repeat. One-GPU
       larger-batch micro8 used GPU 0; micro32/micro64 used GPU 4. All are RTX
       4090s. Parallel GPU 2/3 rates were 10,412 and 18,198 tokens/s for micro32
       and micro64; retain those separately, not as the original-pair controls.
       Changing scientific batch via accumulation alone barely changes FSDP's
       rate. Larger local microbatches improve it strongly: micro64 is 5.57x
       faster than micro8 at the larger global batch, but best two-GPU rate is
       still only 0.685x best one-GPU rate. This does not establish a universal
       FSDP ceiling; overlap/compiler settings were held unchanged, not tuned.
       Micro64 peak live memory: one GPU =19.847 GiB=; two GPUs maximum
       =15.933 GiB/device=. All large-batch configurations share the exact
       scientific settings apart from execution microbatch, core source hashes,
       and per-step token/mask/position/label hashes. Maximum per-step loss
       range across all eight runs =0.00046921= (BF16, not bitwise equality).
       Each rate uses three warm complete mask/transfer/update intervals.
       Concurrent probes can contend for host resources; these remain bounded
       diagnostic measurements, not final repeated paper estimates.
       Full raw/derived evidence: =large-batch-summary.json=, per-run result
       JSON and HLO, =large-batch-topology.txt= and =large-batch-telemetry.csv=,
       under the experiment-15 /raid output root. All workers and the telemetry
       process exited; all six GPUs were free at completion. The four-device
       small-accumulation reshape gate remains unresolved; no core change made.
     - Sequence-length follow-up approved and launched 2026-09-09: compare
       lengths 128, 512 and 1,024 at 131,072 global input tokens/update, seed 7,
       unchanged 505M model, BF16 compute/FP32 AdamW and real WikiText-2 stream.
       Start local microbatch 8 at every length; one GPU (2) and two-GPU FSDP
       (0/1) run concurrently, with sequence lengths sequential within each
       lane. This holds device placement and physical sequence microbatch fixed.
       Four updates per cell; first execution excluded from warm summaries.
       If compiled buffers exceed the configured allocator, preserve the failed
       capacity result and retry BOTH device counts at microbatch 4, then 2.
       Do not interpret mismatched microbatch rows as the causal length control.
       Runner: =experiments/15-modernbert-mlm-scaling/sequence_screen.py=;
       frozen cases remain in =canary.py=. Three configuration/data tests pass,
       including exact input/masking equality across execution microbatches and
       identical original token streams across lengths (masking/context changes
       with sequence length, so numerical parity is tested WITHIN each pair).
       Evidence: =/raid/representax-paper/15-modernbert-mlm-scaling/sequence-screen-20260909/=,
       with commands/PIDs/GPU assignments in =dispatch.json= and per-cell logs,
       HLO, source snapshots, memory and complete optimizer-step observations.
       Completed: ten attempted cells, eight passed (32 finite, non-skipped
       optimizer updates), two rejected by the compiled-memory precheck before
       execution. All 253 core source files match the preceding batch screen.
       | Length | Local microbatch 1/2 GPUs | One-GPU tokens/s | FSDP tokens/s | Ratio |
       |--------+---------------------------+------------------+---------------+-------|
       |    128 |                       8/8 |           24,918 |         3,987 | 0.160 |
       |    512 |                       8/8 |           23,066 |        12,480 | 0.541 |
       |  1,024 |                       2/2 |           17,282 |         7,107 | 0.411 |
       |  1,024 |                       2/8 |           17,282 |        16,270 | 0.941 |
       The last row uses independently sized execution microbatches, not the
       matched-microbatch control; scientific global work remains identical.
       At length 1,024, one-GPU micro8/micro4 require =24.79/26.72 GiB= compiled
       buffers versus the =21.17 GiB= configured allocator budget. These are
       capacity precheck rejections, NOT executed OOMs. Smaller microbatch did
       not monotonically reduce compiler temporary memory. Micro2 fits at
       =18.67 GiB= peak live. Two-GPU micro8/micro4/micro2 all passed, with rates
       =16,270/11,773/7,107 tokens/s= and peak live per device
       =15.06/11.35/8.05 GiB=. FSDP enables the larger tested physical
       microbatch, but does not surpass the observed one-GPU throughput.
       This is capacity evidence, not proof that no other replicated execution
       setting can fit, and not an exhaustive tuning comparison.
       Exact input/masking/position/label hashes match within each length across
       device counts and fitting microbatches. Maximum per-step loss ranges:
       =0.00005627= (128), =0.00006771= (512), =0.00058460= (1,024).
       Startup intervals at length 128 were =11.22/12.54 s= including persistent
       cache loading, NOT cold compilation. New shapes took =154-172 s= on one
       GPU and =116-122 s= on two GPUs. Only three warm observations per rate;
       concurrent lanes can contend for CPU resources. No paper-level stable
       estimate, no core trainer changes and no final nine-run campaign.
       =summary.json= and =report.md= retain the table, all attempted cases,
       parity checks, memory and timing diagnostics. =telemetry.csv= retains
       GPU utilization, memory and power samples. Workers and monitor exited;
       GPU availability checked after completion.
     - Approved follow-up: length 1,024, two-GPU FSDP on GPU 0/1, local
       microbatch 16. Same 131,072 global tokens/update, seed 7, 505M model,
       data/masking, precision and optimizer; four accumulation rounds rather
       than micro8's eight. Four updates planned, three warm observations.
       Fixed case =length-1024-micro16= in =canary.py=; four configuration/data
       tests pass. Compiled-memory precheck remains enabled at the same 90%
       allocator limit. Compare against the saved one-GPU micro2 and two-GPU
       micro8 cells; no core source changes. Artifacts under
       =sequence-screen-20260909/length-1024-micro16-two-gpu= in the same
       experiment-15 /raid root, with its sibling log retained.
       Completed: compiled-memory precheck rejected the executable before any
       optimizer updates. Required =23.5878 GiB/device= versus =21.1660 GiB=
       configured allocator limit; compilation =125.21 s=. No executed OOM,
       throughput or loss-parity measurement for this case. Scientific config,
       model, data provenance and all core source hashes match both saved
       comparators. Best observed length-1,024 two-GPU rate therefore remains
       =16,270 tokens/s= at micro8 versus one-GPU micro2 =17,282 tokens/s=.
       =sequence-screen-20260909/micro16-summary.json= records the capacity
       result and exact baseline artifact hashes; =micro16-telemetry.csv=
       records GPU memory/utilization/power. Worker and monitor exited.
     - Replicated-DDP control approved and launched: compare lengths 128/512
       at local microbatch 8, and length 1,024 at local microbatch 2, against
       the matched saved FSDP and one-GPU cells. Keep 131,072 global input
       tokens/update, seed 7, 505M model, FP32 masters/AdamW, BF16 compute,
       full activation rematerialization, masking and four-update budgets.
       Reuse =ShardingPlan.ddp=; no core-library edits. Frozen =ddp-length-*=
       cases differ only in the canary configuration's sharding field.
       Five configuration/data tests pass, including exact supervision hashes.
       =sequence_screen.py --screen ddp= refreshes one-GPU controls on GPU 2
       concurrently with the two-GPU lane on GPU 0/1; lengths run sequentially
       to preserve the previous PCIe placement and bound compiler contention.
       Compiled-capacity precheck remains enabled; if needed, reduce matched
       microbatches at 128/512 before continuing. No four-GPU launch here.
       Artifacts: =/raid/representax-paper/15-modernbert-mlm-scaling/ddp-screen-20260909/=.
       Completed all six runs and 24 finite/non-skipped optimizer updates;
       three warm observations per run, no compiled-capacity rejections.
       | Length | Local microbatch | One GPU | Two-GPU FSDP | Two-GPU DDP | DDP / one |
       |--------+------------------+---------+--------------+-------------+-----------|
       |    128 |                8 |  24,814 |        3,987 |       6,411 |     0.258 |
       |    512 |                8 |  23,057 |       12,480 |      17,803 |     0.772 |
       |  1,024 |                2 |  17,301 |        7,107 |      10,332 |     0.597 |
       Rates are global input tokens/s. One-GPU controls are refreshed; FSDP
       columns use the saved matched-microbatch GPU 0/1 cases. DDP is
       =1.608/1.427/1.454x= faster than those FSDP controls, but does not recover
       positive strong scaling. This is NOT an independently tuned best-case
       comparison: FSDP's earlier length-1,024 micro8 result remains separate.
       Data, masking, model and all 253 core source hashes match the saved
       FSDP cells. DDP versus refreshed one-GPU maximum loss differences are
       =0.00007343/0.00007534/0.00004959=. Maximum loss range across all three
       strategies is =0.00007725=. No mathematical-supervision changes.
       Optimized HLO has NO parameter all-gathers in these DDP executables,
       but does contain large BF16 gradient all-reduces inside accumulation.
       Loop bodies have =33/32/33= all-reduce-start instructions and run
       =63/15/31= times after the first microbatch outside the loop. This
       verifies repeated gradient synchronization, not a measured attribution
       of elapsed time to communication. Next proposed core change, pending
       approval: accumulate local gradients and synchronize after accumulation,
       preserving correct global masked-token normalization and numerical
       behavior. Do not infer an unavoidable hardware/JAX ceiling from this
       current schedule. No core training changes made for this screen.
       DDP cold compilations: =164.24/173.94/158.10 s=. One-GPU cache reuse:
       =11.37/11.29/11.51 s= (not cold compilation). DDP peak live memory:
       =10.41/19.82/19.05 GiB/device=. Complete step intervals include host
       masking and device placement. The refreshed single-GPU rates remain
       within 0.5% of their saved controls, but these remain short canaries.
       =summary.json=, =report.md= and =collectives.json= retain results,
       baseline hashes, parity checks, exact HLO line references and limits.
       Raw per-step logs, HLO, source snapshots and =telemetry.csv= are kept.
       All workers and the telemetry monitor exited; GPUs checked after exit.
     - Follow-up approved 2026-09-10: seed-7 weak and strong DDP screens.
       Frozen =weak-ddp-{1,2,4}gpu= cases use length 512, local batch 8,
       no accumulation and global batches 8/16/32. This is explicitly weak
       scaling, not evidence of faster execution at fixed global work.
       =strong-ddp= keeps 131,072 tokens/update (256 sequences), local batch 8
       and 32/16/8 accumulation rounds on 1/2/4 GPUs. Each screen uses 21
       updates, with the first excluded from the 20-observation warm summary.
       The strong screen explicitly repeats the seeded WikiText-2 stream when
       its 2,752,512 requested tokens exceed the corpus; every topology sees
       the same token stream and masking. No silent data truncation.
       Weak workers launched at =weak-ddp-20260910/= under experiment 15's
       /raid output root. GPU 2 and GPUs 0/1 run concurrently, then 0/1/2/3.
       =scaling_screen.py= records commands, PIDs, telemetry and results.
       Strong launch is gated on loss/update parity and compiled HLO showing
       deferred gradient synchronization. The proposed implementation uses
       public JAX reduced/unreduced shardings, not manual device collectives.
       Core changes are restricted to explicit DDP with accumulation; the
       explicit accumulation reshape also receives its output sharding before
       reshaping, addressing the prior four-device readiness failure.
       Weak screen completed: 63 finite/non-skipped updates, 20 warm per run.
       | GPUs | Global batch | Tokens/s | Speedup | Efficiency |
       |------+--------------+----------+---------+------------|
       |    1 |            8 |   11,161 |   1.000 |     100.0% |
       |    2 |           16 |   13,909 |   1.246 |      62.3% |
       |    4 |           32 |   17,369 |   1.556 |      38.9% |
       Weak step-time CV is 0.28-0.30%; cold compilations 87-91 seconds.
       Compiled live requirements are 19.50-19.52 GiB/device. This is
       positive but sublinear weak scaling; global batches/trajectories differ.
       Strong screen completed in =strong-ddp-20260910/=: 63 finite/non-skipped
       updates, 20 warm per run, fixed 131,072 tokens (256 sequences)/update.
       | GPUs | Accumulation | Tokens/s | Speedup | Efficiency | Seconds/update |
       |------+--------------+----------+---------+------------+----------------|
       |    1 |           32 |   22,992 |   1.000 |     100.0% |         5.7007 |
       |    2 |           16 |   37,827 |   1.645 |      82.3% |         3.4651 |
       |    4 |            8 |   45,962 |   1.999 |      50.0% |         2.8517 |
       This recovers positive strong scaling, not linear four-device scaling.
       Compared with the earlier short two-GPU DDP control (17,803 tokens/s),
       the new two-GPU rate is 2.125x; timing windows differ (20 versus 3 warm
       observations). Current one-GPU control remains within 0.3% of the prior
       23,057 tokens/s control. No claim of measured interconnect bandwidth.
       Strong warm step-time CV is 0.22-0.47%; cold compilation is 173-175 s
       per executable. Compiled memory is 19.87 GiB/device at all three counts.
       Strong input/masking hashes match; 1/2-GPU maximum loss difference is
       0.00241995 over 21 BF16 updates. This is not bitwise trajectory parity.
       Across all three device counts, maximum absolute loss range is
       0.00660896. Final losses are 7.235440/7.233020/7.238713. FP32 toy
       loss/update parity passes unchanged tolerances; BF16 trajectories are
       close, not identical. This is a single-seed systems screen, not final
       three-seed evidence or a converged-pretraining result.
       Two-GPU HLO has no million-element-or-larger gradient reduction inside
       accumulation; matrix gradient reductions moved after the scan. Smaller
       normalization-vector and scalar reductions remain, explicitly retained
       because JAX reduced values cannot enter ordinary elementwise broadcasts.
       Core regression checks: 49 CPU tests passed, a dedicated compiled-HLO
       matrix-reduction placement test passed, and two GPU parity tests passed.
       Initial test failures were reduced-vector typing and reduced-scalar
       weighting requirements; both resolved without loosening tolerances.
       Selected-parameter, empty-target and frozen-weight tests also pass.
       =report_scaling.py RUN_DIR= rebuilds =aggregate.json= and =report.md=.
       Both distributed strong executables passed the large-gradient HLO gate.
       Additional BF16 precision checks and the seven configuration/data/HLO
       parser tests pass; Ruff, type checks and git diff whitespace checks pass.
       All six runs (126 updates total), dispatchers and telemetry processes
       exited normally. All six GPUs verified idle after completion.
     - User-approved synchronization cleanup, 2026-09-10: removed the two
       accumulation parameter/gradient sharding arguments and the preset-name
       eligibility flag. The existing trace-local data-axis context and actual
       parameter layouts now determine whether reductions can be deferred.
       The step differentiates a reduced view of replicated parameters,
       accumulates local contributions, then synchronizes before clipping and
       AdamW. The optimizer uses the original parameter tree; no restoration
       cast, extra mesh axis or new experiment setting is needed.
       Local, automatic-sharding and non-replicated parameter paths retain
       their existing behavior. A custom replicated layout also receives the
       same optimization, independent of its strategy name.
       Verification: 44 focused CPU tests passed (four emulated devices),
       including DDP/FSDP updates, selected/frozen parameters and empty targets.
       Two compiled-HLO checks passed for DDP and custom replicated layouts:
       large matrix-gradient reductions remain outside the accumulation loop.
       Before/after two-device toy ModernBERT MLM controls have byte-identical
       StableHLO and all 63 returned array leaves identical in both FP32 and
       BF16. This establishes unchanged lowering for those controls, not a new
       full-model throughput measurement. Ruff, type and whitespace checks pass.
       Evidence: =accumulation-cleanup-20260910/{before,after,comparison.json}=
       under experiment 15's /raid root. The initial failed validation log is
       retained: reconstruction of an unreduced PartitionSpec required its
       explicit =partitions= field; corrected without tolerance changes.
       No benchmark configurations changed or new GPU training runs launched.
       Follow-up naming cleanup: rebind =trainable_model= only inside
       =differentiated_loss=; the outer optimizer-facing tree is unchanged.
       Removed =gradient_model=. All 45 focused CPU tests pass, including both
       reduction-placement checks. FP32/BF16 toy controls retain identical
       returned arrays and optimized opcode/all-reduce counts, but IR text is
       not byte-identical after moving the annotation into the helper. This is
       not a new GPU timing measurement. Evidence is in the same root under
       =local-rebinding/=. Ruff and type checks pass.
     - Approved next screen, 2026-09-10: tune physical microbatch 8/16/32/64
       on one GPU before comparing 1/2/4-device DDP. Keep the 505M model,
       length 512, 131,072 tokens/update, seed 7, precision, checkpointing,
       optimizer, data and 21-update schedule unchanged. Stop increasing the
       microbatch at the compiled allocator-capacity boundary. Pick the highest
       measured one-GPU warm rate, then run two/four GPUs concurrently on
       disjoint devices, reducing accumulation at fixed global work.
       =experiments/15-modernbert-mlm-scaling/tune.py= records the selection,
       commands, telemetry, all rejected cases and the matched summary.
       Output: =/raid/representax-paper/15-modernbert-mlm-scaling/tuned-ddp-20260910/=.
       This remains a single-seed tuning/systems screen; do not label it the
       completed nine-run panel or a proven global performance optimum.
       Completed under commit =bd0d61a=, following MLM commit =f94212b= and
       synchronization commit =d708908=. The single-GPU physical-batch sweep:
       | Microbatch | Warm tokens/s | Compiled GiB | Outcome |
       |------------+---------------+--------------+---------|
       |          8 |        23,489 |        19.87 | Selected |
       |         16 |        22,744 |        20.70 | Passed, 3.2% slower |
       |         32 |             - |        32.62 | Rejected before execution |
       |         64 |             - |            - | Skipped after capacity boundary |
       Allocator limit was 21.17 GiB/device; microbatch 32 also exceeds the
       physical 24 GB GPU. This does not establish an exact maximum batch.
       Cached microbatch-8 compile/load was 11.36 seconds; microbatch 16 cold
       compilation took 175.98 seconds. Actual live peaks were 19.87/20.70 GiB.
       Matched seed-7 strong scaling, 21 updates each with 20 warm observations:
       | GPUs | Accumulation | Tokens/s | Speedup | Efficiency | Seconds/update |
       |------+--------------+----------+---------+------------+----------------|
       |    1 |           32 |   23,489 |   1.000 |     100.0% |         5.5801 |
       |    2 |           16 |   37,847 |   1.611 |      80.6% |         3.4632 |
       |    4 |            8 |   46,458 |   1.978 |      49.4% |         2.8213 |
       Two/four-GPU cold compilation was 175.25/173.04 seconds; compiled
       memory remained 19.87 GiB/device. Step-time CV was 0.04/0.65/0.94%.
       All 84 executed updates (including the microbatch-16 candidate) were
       finite/non-skipped. Tokens, corruption, labels and masked-position
       hashes match across topologies. Initial losses were 10.620912/10.620914/
       10.620913; final losses 7.250642/7.232751/7.239561. Maximum loss range
       is 0.0178914, not bitwise BF16 trajectory equivalence. Both distributed
       executables passed the large-gradient reduction-placement gate.
       This confirms useful but sublinear scaling from the fastest tested
       single-GPU baseline. Eight experiment configuration tests pass.
       =selection.json=, =dispatch.json=, =aggregate.json=, =report.md=, raw
       steps, HLO, compiled/live memory and telemetry are retained. The selected
       one-GPU result is reused in the scaling table, not an independent rerun.
       All workers and telemetry exited; all six GPUs verified idle afterward.
     Run only Representax with the controlled ModernBERT encoder and a real text
     corpus for seeds =7, 42, 773= at 1, 2 and 4 GPUs. Hold the global token
     batch and optimizer work fixed, using the same replicated DDP policy at
     each device count. The length-512 fixed-work recipe supersedes the older
     FSDP-only, two-length proposal for this panel. Report optimizer steps/s,
     tokens/s, scaling efficiency, compilation, peak HBM and observed
     collective placement. This is a bounded
     pretraining-like systems result, not a claim to reproduce full BERT
     pretraining or downstream quality.

     Separately run one deterministic capacity probe for a model or batch that
     fails under replication but trains under FSDP or hybrid data/model
     sharding. Feature this result in the main scaling section as evidence that
     JAX exposes useful sharding choices; do not count it as another seeded
     scaling cell.
**** DONE Accept canonical LeJEPA bounded lifecycle on ImageNet-1K against stable-pretraining
     Use two global and six local views, the paper's convex loss weighting and
     per-view SIGReg, a distinct projector used only for training, and frozen
     backbone features for evaluation. The previous two-view toy acceptance
     incorrectly centered over all views; that test could not reveal the error
     because all views were global. The corrected eight-view objective matches
     the exact paper value and projection gradient against the PyTorch oracle;
     both workers load the same 28,168,576-parameter ViT-S/16 initialization.
     Real ImageNet-1K images pass native and reference updates, exact native
     checkpoint/optimizer restore, cursor/RNG resume, export/reload, k-NN,
     linear probing and exact reloaded-evaluator reproduction. This is bounded
     lifecycle readiness, not an ImageNet quality trajectory. Compact evidence
     lives in =benchmarks/results/lejepa-acceptance-20260831/=; the promoted
     ViT-L/16 capacity and schedule remain in the separate manifest-freeze item
     above. Official validation provenance is accepted separately in
     =benchmarks/results/imagenet-validation-provenance-20260831/=.
**** DONE Complete the seed-7 30-step promotion screen
     Run the seven remaining paired workloads for 30 updates on one RTX 4090
     per worker. The primary systems comparison excludes native compilation
     rows and explicit device-input idle, and uses CUDA-synchronized reference
     optimizer-step timing. Native full-loop throughput and input idle remain
     visible rather than being discarded. These are promotion screens, not
     convergence or final paper evidence.

     | Workload             | Representax active ex/s | Reference ex/s | Ratio | Representax full-loop ex/s | Input idle | Final metric: Representax / reference |
     |----------------------+--------------------------+----------------+-------+-----------------------------+------------+----------------------------------------|
     | Cross-encoder        |                   607.95 |         460.86 | 1.32x |                      469.28 |      22.8% | 0.61163 / 0.61163                      |
     | Late interaction     |                   130.08 |          54.86 | 2.37x |                      125.32 |       3.7% | 0.69788 / 0.70712                      |
     | Outcome reward       |                     7.85 |           6.65 | 1.18x |                        7.85 |       0.0% | 0.59375 / 0.62500                      |
     | Process reward       |                    55.51 |          15.25 | 3.64x |                       52.93 |       4.6% | 0.85938 / 0.88281                      |
     | Image-text retrieval |                   271.73 |         104.68 | 2.60x |                      110.28 |      59.4% | 0.77757 / 0.77711                      |
     | Audio-text retrieval |                     2.69 |           1.82 | 1.48x |                        2.69 |       0.0% | 0.86879 / 0.85685                      |
     | Video-text retrieval |                     3.59 |           1.52 | 2.36x |                        3.50 |       2.5% | 1.00000 / 1.00000                      |

     The metrics are TREC DL 2019 nDCG@10, NanoMSMARCO nDCG@10,
     UltraFeedback pair accuracy, Math Shepherd step accuracy, Flickr30k
     nDCG@10, AudioCaps nDCG@10 and MSR-VTT nDCG@10, respectively. The
     eight-query video panel checks execution only. Outcome-reward scalar heads
     have framework-native seeded initialization, so its 30-step quality delta
     is not a parameter-identical comparison. Image-text compute is green, but
     its 59.4% native input-idle share must be fixed before the serious run.

     All rows pass checkpoint, export and inference-reload acceptance. The
     sweep exposed and fixed an undeclared =peft= dependency in the shared
     reference environment and a video reload probe that incorrectly scaled to
     the full training batch and exhausted memory after training. Exact reports,
     component paths, timing inputs and Sentence Transformers, PyLate and TRL
     commits are recorded in
     =/raid/representax-paper/seed-7-30-step-screen/summary.json=; the rendered
     table is =/raid/representax-paper/seed-7-30-step-screen/results.md=.
**** DONE Complete the five-seed 20-update framework decision sweep
     All 65 paired runs are complete: 13 implemented comparison recipes, five
     seeds (=7, 42, 773, 1234, 2026=), and 20 optimizer updates in each
     framework. This is 100 updates per framework per recipe and 2,600 updates
     across the accepted runs. Primary throughput is measured from optimizer
     completion to optimizer completion, including input wait. First-use
     compilation and intervals containing midpoint checkpoint or resume work
     remain in the raw logs but are excluded from steady-state rates.

     | Recipe                             | Median Representax/reference ratio | Loss direction |
     |------------------------------------+-------------------------------------+----------------|
     | Dense retrieval                    |                              0.740x | 5/5 agree      |
     | Semantic similarity, MPNet         |                              1.172x | 5/5 agree      |
     | Semantic similarity, BERT Base     |                              0.673x | 5/5 agree      |
     | Pair classification, MPNet         |                              1.082x | 5/5 agree      |
     | Pair classification, BERT Base     |                              0.666x | 5/5 agree      |
     | Cross-encoder                      |                              1.705x | 5/5 agree      |
     | Late interaction                   |                              2.452x | not comparable |
     | Outcome reward                     |                              1.175x | 5/5 agree      |
     | Process reward                     |                              3.309x | 5/5 agree      |
     | Image-text                         |                              3.083x | 5/5 agree      |
     | Audio-text                         |                              1.500x | 5/5 agree      |
     | Video-text                         |                              2.447x | 5/5 agree      |
     | V-JEPA 2.1                         |                              5.285x | 5/5 agree      |

     Representax is faster in 10 of 13 median recipe comparisons. The losses
     are optimized dense MPNet against TorchInductor and both BERT Base
     controls. Every loss-comparable recipe moves in the same direction in all
     five seeds. Late interaction exposes different framework reductions and
     opposite raw loss directions, so its speed result requires held-out quality
     before promotion. Median Representax input wait is below 0.1% in every
     recipe after bounded prefetch and lazy preprocessing, so the remaining
     losses are model/compiler execution gaps rather than data starvation.

     These 20-update traces establish steady execution and short loss dynamics,
     not converged held-out quality. Continue toward a selective systems paper,
     not a universal speed claim. Promote the winning rows only after longer
     shared-evaluator quality runs; retain dense and BERT as explicit limits.
     LeJEPA is omitted because its current reference is an experiment-owned
     timm oracle using stable-pretraining components rather than the external
     stable-pretraining loop. NQ and MIRACL remain lifecycle checks without
     paired external trainers.

     The public launcher is
     =experiments/08-five-seed-framework-comparison/run.py=. Aggregate JSON,
     rendered results, raw logs, per-step metrics, per-seed summaries and
     content-addressed reference provenance live under
     =/raid/representax-paper/08-five-seed-framework-comparison/=.
     On September 6, the reproducible checkpoints, exports, compiler caches,
     media tensors and failed-attempt trees were deleted after verifying all 65
     canonical summaries byte-for-byte. The retained evidence is 1.1 GB rather
     than 777 GB; =PRUNING-MANIFEST.json= records every canonical summary hash.
**** DONE Repeat the five-seed framework sweep across TPU and refreshed GPU controls
     Put the reproducible launcher under
     =experiments/10-cross-accelerator-framework-comparison/= and durable output
     under the matching =/raid/representax-paper/= directory. Run the 13 concrete
     recipes for seeds =7, 42, 773, 1234, 2026=, with 20 measured optimizer
     updates per framework and seed after first-use compilation. Report complete
     optimizer steps/s and examples/s including input wait; retain input wait,
     non-padding tokens/s, padded positions, compilation, memory, loss and all
     raw step intervals separately.

     Preserve one resolved =run.json=, append-only =metrics.jsonl= and
     =events.jsonl= streams, the complete process log, environment and package
     inventory, command, source patch, and data manifest for every process.
     Record per-step wall time, examples, valid and padded tokens or modality
     units, shape/bucket, loss components, gradient/update norms, input wait,
     preprocessing, placement, compilation/first use, checkpoint/evaluation
     boundaries, peak host/device memory, utilization and power where available.
     Treat =summary.json= and rendered tables as rebuildable products of those
     raw files so later analyses can compute alternative warmup rules, quantiles,
     variance, energy per example, shape-conditioned rates and scaling efficiency.

     On TPU, run Representax and the applicable PyTorch/XLA reference
     sequentially on the full =v5litepod-16= slice. This allocation is already a
     multi-host TPU test; broader multi-node GPU work remains future work because
     of cost. Canary each concrete integration before admitting it to the table.
     Use matched static text buckets on TPU, global batch 64 and actual length
     2,048 for process reward, and global batch 128 with a full 20-update SSV2
     exposure for V-JEPA rather than the old tiny lifecycle probes.

     Rerun the corresponding GPU recipes on the six-RTX-4090 host, including
     eager and TorchInductor reference controls where supported. Use native
     dynamic padding for eager PyTorch references and the accepted static bucket
     schedule for Representax; retain a matched-shape control where padding could
     explain a verdict. If any seed supplies fewer than 15 valid warm intervals
     or has unusually unstable timing, stop and ask before extending either
     framework to 40 updates. Record exact source commits and patches, dependency
     versions and locks, data revisions and hashes, commands, environment,
     topology and device inventory in every result bundle.
***** DONE Establish the locked 16-chip multi-host TPU preflight
      The =v5litepod-16= slice is healthy and exposes a 4x4 ICI torus through
      four JAX processes with four local chips each. A compiled 16 MiB-per-chip
      all-reduce measured 56.19 GB/s payload rate within each worker's 2x2 tile
      and 39.86 GB/s over all 16 chips; these are effective collective payload
      rates, not raw link-bandwidth claims.

      Separate locked environments now install Representax/JAX on Python 3.13
      and Sentence Transformers/PyTorch-XLA on system Python 3.10 without
      unused CUDA wheels. An identical FP32 1024-to-256 dense projection trained
      for 20 updates on both stacks with final losses 0.198249 and 0.198347.
      The substrate smoke measured 16.83M examples/s for JAX and 2.32M for
      PyTorch/XLA, but this simple projection is not a transformer speed claim.
      The production =run_job= path also completed 20 symmetric-MNR updates
      with DDP over all 16 chips, global batch 256, and rematerialized GradCache;
      loss moved from 2.96009 to 2.84377 at a 10,258 examples/s median warm rate.

      Code is commit =a837274eeb6634f898b3e5b34bf878b32fdd07a0= on
      =codex/tpu-multihost-preflight=. Full logs and evidence live under
      =/raid/representax-paper/10-cross-accelerator-framework-comparison/preflight/tpu-v5e-16-20260905/=.
***** DONE Run the actual Sentence Transformers trainer over the 16-chip slice
      Sentence Transformers 5.6.1 through Accelerate 1.14 and PyTorch/XLA 2.9
      completed 20 FP32 MNR updates with a pinned 4.39M-parameter BERT model,
      local batch 16 and global batch 256. Loss moved from 1.77237 to 0.970649
      in 53.83 seconds, including first compilation and input work. Accelerate
      reported =DistributedType.XLA=, and a post-training parameter probe had
      zero spread over all 16 replicas, confirming synchronized updates.

      The canary uses per-device in-batch negatives. Accelerate synchronizes
      model gradients through XLA, but Sentence Transformers' optional
      cross-device-negative helper requires an initialized =torch.distributed=
      process group. Initializing PyTorch/XLA's documented =xla://= process
      group and enabling =gather_across_devices= halted the TPU during the
      first BERT forward, both with and without Trainer step logging. The
      off-the-shelf Sentence Transformers/Accelerate XLA path therefore does
      not currently support this global-negative multi-host comparison. Do not
      compare it against Representax global negatives: either run both with
      per-device negative pools and label that objective explicitly, or omit
      the dense TPU row while retaining the matched GPU result. This result
      proves the maintained Trainer executes coherently, not final MNR
      scientific parity or paper throughput. Evidence is under
      =/raid/representax-paper/10-cross-accelerator-framework-comparison/preflight/sentence-transformers-tpu-20260905/=.
***** DONE Complete the five-seed 16-chip TPU framework sweep
      All 13 recipes completed for seeds =7, 42, 773, 1234, 2026= in both
      frameworks: 130 canonical processes and 2,600 measured optimizer updates.
      Every process has 20 finite complete-step intervals and a finite loss.
      Rates are total examples divided by those 20 completion-to-completion
      intervals, including input stalls, followed by the median across seeds.

      | Recipe                              | Representax examples/s | Reference examples/s | Ratio |
      |-------------------------------------+------------------------+----------------------+-------|
      | Dense retrieval                     |               2510.213 |             2506.150 | 1.002 |
      | Semantic similarity, MPNet Base     |               4045.865 |              808.686 | 5.003 |
      | Semantic similarity, BERT Base      |               3439.733 |              829.874 | 4.145 |
      | Pair classification, MPNet Base     |               4124.540 |              773.708 | 5.331 |
      | Pair classification, BERT Base      |               3478.858 |              758.164 | 4.589 |
      | Cross-encoder                       |               2701.598 |              807.870 | 3.344 |
      | Late interaction                    |               2765.341 |             1059.692 | 2.610 |
      | Outcome reward                      |                103.871 |               59.927 | 1.733 |
      | Process reward                      |                 85.259 |               24.311 | 3.507 |
      | Image-text                          |               2259.882 |             1062.058 | 2.128 |
      | Audio-text                          |                 11.247 |               13.451 | 0.836 |
      | Video-text                          |                 80.699 |                8.465 | 9.534 |
      | V-JEPA 2.1                          |                 87.589 |               59.633 | 1.469 |

      Representax is faster in 11 rows, dense retrieval is effectively tied,
      and PyTorch/XLA is 1.196x faster for audio-text. These are bounded
      cross-accelerator execution, throughput and short numerical-trajectory
      results, not converged-quality or universal-speedup claims. Dense retrieval
      uses matched per-device negative pools because the maintained Sentence
      Transformers multi-host TPU path cannot gather global negatives. Late
      interaction's framework-native losses have different reductions, so its
      throughput must not be promoted without a shared held-out evaluator.

      Preserve two data-cache order effects in interpretation. Image-text native
      seed 7 measured 974.665 examples/s versus 2259.760--2311.757 for the four
      later seeds. V-JEPA native seed 7 measured 43.085 examples/s versus
      85.269--88.134 later. Their reference runners materialize tensors before
      starting the timed loop, while =run_job= streams them; seed 7 therefore paid
      cold filesystem reads. The median remains a useful warm systems estimate,
      but these rows require randomized process order or explicit cache control
      before a final hardware claim. No run was silently replaced or extended to
      40 updates.

      Initial 3B reference capacity probes failed at global batch 64 for audio
      and 128 for video when PyTorch/XLA could not reserve another 6.42 GB and
      6.59 GB respectively. The accepted comparisons use global batch 48 for
      audio and 112 for video in both frameworks; the rejected attempts remain
      in the worker artifact trees.

      Three video reference attempts were retained after =libtpu= terminated in
      runtime metric aggregation following consecutive 3B-model processes. Guest
      reboots cleared that process-runtime state and all five canonical seeds then
      completed with matching loss behavior. Treat this as a PyTorch/XLA runtime
      reliability caveat, not a model or numerical failure.

      The final recipe fixes are commits =924453f=, =63207d0=, =3dc2ff2=,
      =5b0d216=, =1471cbc= and =3491fb0=. Reference provenance pins
      Sentence Transformers =7d3eb16a65f62045226e08082ade63cbc71c97a4=,
      PyLate =346ea2b9c273256919809ac35b3a055970780c9c=, TRL
      =a7be897f5c8d7b52161f9f8a47d8e6242456b898= and V-JEPA 2
      =204698b45b3712590f06245fbfba32d3be539812=. Every result bundle also
      records package versions, source patch, command, environment and data hash.

      The verified four-worker raw copy and aggregate live under
      =/raid/representax-paper/10-cross-accelerator-framework-comparison/tpu-v5e-16/=.
      =results.json= has SHA-256
      =67d6d42bf0abb7a8c87fc59937d0eb559460e5d0203a089666f5df98025e1ccc=;
      =RESULTS.md= has SHA-256
      =5112d5931d1dd932ea293aefa2e2a5f5d45e6c9bfa3892ced45703ffb6a44c2d=.
***** DONE Complete the five-seed RTX 4090 framework sweep
      All 13 recipes completed for seeds =7, 42, 773, 1234, 2026= in both
      frameworks: 130 canonical processes. Each run executes 22 optimizer
      updates and the aggregate uses the final 20 non-compilation intervals;
      the native late-interaction rows contribute 15 valid intervals after
      excluding first use for seven bucket signatures, which meets the frozen
      acceptance threshold. No run was extended or silently replaced.

      | Recipe                              | Representax examples/s | Reference examples/s | Ratio | Median loss correlation |
      |-------------------------------------+------------------------+----------------------+-------+-------------------------|
      | Dense retrieval                     |                305.348 |              211.222 | 1.446 |                   0.966 |
      | Semantic similarity, MPNet Base     |                559.720 |              367.802 | 1.522 |                   0.996 |
      | Semantic similarity, BERT Base      |                286.211 |              384.077 | 0.745 |                   0.999 |
      | Pair classification, MPNet Base     |                552.810 |              361.943 | 1.527 |                   1.000 |
      | Pair classification, BERT Base      |                296.207 |              382.917 | 0.774 |                   0.998 |
      | Cross-encoder                       |                670.369 |              441.531 | 1.518 |                   0.987 |
      | Late interaction                    |                262.290 |               40.455 | 6.484 |          not comparable |
      | Outcome reward                      |                  8.001 |                6.513 | 1.229 |                   0.796 |
      | Process reward                      |                 56.229 |                6.244 | 9.005 |                   0.938 |
      | Image-text                          |                321.020 |              106.223 | 3.022 |                   1.000 |
      | Audio-text                          |                  2.660 |                1.845 | 1.442 |                   0.995 |
      | Video-text                          |                  3.482 |                1.490 | 2.337 |                   1.000 |
      | V-JEPA 2.1                          |                  6.571 |                1.304 | 5.040 |                   1.000 |

      Rates are total examples divided by complete optimizer-step time,
      including input wait, followed by the median across seeds. Compilation,
      checkpoint, evaluation and export intervals remain recorded separately.
      Representax is faster in 11 of 13 eager/reference rows. The two losses are
      the BERT Base sentence-pair controls. Ten recipes favor Representax on
      both RTX 4090 and v5e TPU; BERT Base favors Representax only on TPU, while
      audio-text favors it only on GPU. This supports a portable-systems result,
      not a universal speed claim.

      The additional dense TorchInductor control reaches 316.610 examples/s,
      making Representax 0.964x as fast as the compiled reference while remaining
      1.446x as fast as eager Sentence Transformers. All comparable recipes move
      loss in the same direction for all five seeds. Late interaction retains a
      throughput result only because its framework-native loss reductions differ;
      it requires shared held-out quality before any optimization claim.

      The complete aggregate passes status, content-hash, finite-timing and
      minimum-observation validation for all 130 processes. Runs capture clean
      Representax commit =7b7d27e3d8a61197b9a4e88094a096ff17192be5= and the
      pinned Sentence Transformers, PyLate, TRL and V-JEPA revisions above.
      Single-process canonical-log promotion and TRL loss preservation were
      repaired afterward in commits =cb1b50b=, =a194e36=, =a0da720= and
      =3b97c2b= without rerunning or changing measured values.

      Canonical evidence is under
      =/raid/representax-paper/10-cross-accelerator-framework-comparison/gpu-rtx4090/=;
      its =results.json= SHA-256 is
      =f140459f361239659e75029fe27ae6e1d23aeb7b62327b711f8f6d72d6ef62bc=
      and =RESULTS.md= SHA-256 is
      =ff67fdbe7aa682ed94a7576e76ab366d0a1226034127e16b2bcd911df39a86b9=.
      The five-seed Inductor control is under the adjacent
      =gpu-rtx4090-torchinductor/= directory; its aggregate hashes are
      =8a9af905f7649c463b7a797b1a8c4726d66a41d2aaf50df049dc890f999bb878=
      and =e5a97eb482ffa1208dba5aedd3dd0f9a198bd00115503102b0f6d57d5aeea2cc=.
**** DONE Enforce duplicate-free batches for in-batch-negative objectives
     Materialize one query-positive pair per query, deterministically shuffle
     across enough source shards, and reject any MNR batch containing duplicate
     positives. Preserve the mined-negative column only for objectives that use
     explicit negatives. Add batch-level uniqueness statistics to every dense
     experiment manifest before another MNR or BERT-scaling campaign is run.
     The cross-accelerator dense runner now consumes five deterministic,
     seed-specific 65,536-row tables with unique queries and positives. Every
     accepted run reports zero duplicate queries and zero duplicate positives.
     The pinned manifest SHA-256 is
     =daa2b78def311e831435b22281441770ceeec0222c382ae54479166d48f1750e=.

     The September 7 data-path audit extended this invariant to late interaction
     and media retrieval. Late-interaction preparation now selects unique queries
     and positives across the source stream. COCO, AudioCaps and MSR-VTT
     preparation skips repeated media identifiers and text targets; COCO uses four
     distinct captions per image instead of cloning the first caption. Finite
     datasets are repeated only by the canonical =run_job= loader to satisfy
     =max_steps=, not by copying rows during preparation. Paired dense launchers
     now require and record the duplicate-free seed parquet explicitly.

*** TODO Publish reproducible configs, manifests, tests, and benchmark artifacts :research:reproducibility:
    Numbered launchers own executable settings; retain shared instructions here.
    From the repository root, using the environment installed by
    =experiments/setup.sh=:
    #+begin_src sh
    experiments/02-semantic-similarity-pair-classification/run.sh -g 0,1,2,3,4,5
    experiments/.venv/bin/python experiments/08-five-seed-framework-comparison/run.py run --gpus 0 1 2 3 4 5
    experiments/.venv/bin/python experiments/08-five-seed-framework-comparison/run.py aggregate
    experiments/.venv/bin/python experiments/09-modernbert-dense-retrieval/run.py run --gpus 0 1 2 3 4 5
    #+end_src
    These commands use the current checked-in recipes. Historical measurements
    retain their original data, seeds and source snapshots; do not relabel them
    when a launcher changes. The aggregate command performs no training.
*** TODO Write the paper on the portable JAX representation-training system :research:paper:
*** TODO Release the Representax systems paper as an arXiv preprint :research:paper:
**** TODO Delete retained Google Cloud paper assets after the final preprint is published
     Once the final preprint and reproducibility package are complete, verify
     that every required manifest and result is durable under =/raid=, then
     delete =gs://representax-paper-assets-project-3fed1b4b=. Confirm that the
     bucket, including soft-deleted objects, is gone and that the project has no
     remaining billable resources.
** Profilax paper: =So you like to go fast?= [0/15] [0%]
  Profilax is a separate installable JAX profiling and semantics-preserving
  execution-planning project. Its paper must demonstrate portability beyond
  Representax. Build and validate the independent core first; only then propose
  focused upstream integrations to maintained systems such as MaxText rather
  than asking those projects to absorb an unproven abstraction.

*** TODO Create =profilax= as a separate installable JAX project :profilax:
*** TODO Profile arbitrary compiled JAX programs through a small adapter :profilax:
*** TODO Inventory devices, topology, memory, and compiler versions :profilax:
*** TODO Search only caller-declared execution parameters under immutable scientific constraints :profilax:
*** TODO Measure compilation separately from warmup and steady-state execution :profilax:
*** TODO Reject plans that alter the scientific projection or violate numerical tolerances :profilax:
*** TODO Report structured infeasibility when the workload requires more accelerators :profilax:
*** TODO Select Pareto-optimal plans for throughput, memory, and compile cost :profilax:
*** TODO Cache plans by program, shapes, hardware, and JAX/XLA versions :profilax:
*** TODO Guarantee feasibility for accepted plans under a fingerprinted environment :profilax:correctness:
   Combine analytic tensor/liveness estimates with measured compiled-program
   profiles and a configurable safety margin. Guarantee only the admitted
   compiled workload under that exact fingerprint; external storage, decoding,
   drivers, and runtime faults remain outside the contract.
*** TODO Integrate Profilax with Representax :profilax:integration:
*** TODO Demonstrate a second independent downstream JAX integration :profilax:integration:
*** TODO Prepare and submit focused upstream PRs after independent validation :profilax:integration:
   Start with maintained projects whose owners accept the use case, such as
   MaxText, and keep each PR small: profiling adapter, plan import, or telemetry
   rather than coupling the downstream trainer to Profilax internals.
*** TODO Benchmark Profilax across multiple downstream JAX programs :profilax:benchmark:
*** TODO Write and release =So you like to go fast?= :profilax:research:paper:

* Initial Representax roadmap [0/42] [0%]
** TODO Grow verified native model families from representation-learning needs :models:hf:
  Keep the pinned Transformers 5.3.0 catalogue as a versioned compatibility
  index and code-generation input, not as a promise to backfill every
  =AutoModel=. Add a native family when a representation task, modality, model,
  or research result requires it, and claim support only after the complete
  numerical, gradient, export, and systems gates pass.
  The first acceptance panel spans BERT-family embedders such as MiniLM or BGE,
  MPNet, ModernBERT/ModernVBERT, SigLIP2, and Jina v5 Omni. Grow beyond it
  organically through multilingual XLM-R-style encoders, modern decoder
  embedders, audio and video towers, state-space models, and mixture-of-experts
  only when they create a concrete capability or scientific comparison.
  Jina v5 Omni's CC-BY-NC-4.0 weights remain an external compatibility oracle;
  native code support must not imply that Representax redistributes those
  weights or grants commercial rights to them.
*** TODO Build reusable native transformer and modality components :models:
**** TODO Generalize positional, residual, MLP, and layer-stack policies through another family
    Extract a policy only when at least two native consumers demonstrate the
    same semantics; do not force superficially similar model graphs together.
*** TODO Port encoder-only text families :models:text:
**** TODO Implement and accept BERT decoder cache and cross-attention modes :models:bert:
*** TODO Port decoder-only, state-space, and mixture-of-experts text families :models:text:
*** TODO Port encoder-decoder text families :models:text:
*** TODO Port vision and image-representation families :models:vision:
*** TODO Port audio and speech representation families :models:audio:
*** TODO Port video representation families :models:video:
*** TODO Port multimodal and composite families :models:multimodal:
**** TODO Add Qwen3-Omni checkpoint, export, and training support :models:multimodal:future:
*** TODO Port remaining time-series, graph, table, and specialist families :models:
** TODO Unify native and Hugging Face loading without duplicate weights :models:hf:export:
  Expose one public model loader that detects a Representax bundle, local Hugging
  Face directory, or Hub reference. Reuse standard =config.json=,
  Safetensors, tokenizer, and processor assets, while keeping Representax-only
  pooling, route, prompt, precision, and task metadata in a small manifest.
  Construct a typed weightless Equinox template from architecture metadata and
  materialize the selected native weights exactly once; never reread a source
  checkpoint merely to obtain tree structure before deserializing an export.
  Memory mapping or lazy Safetensors reads may bound host memory, but the loader
  must explicitly place every executable JAX array in its destination sharding.
** TODO Export trained models back to Hugging Face-compatible artifacts :models:hf:
** TODO Test tokenizer and processor parity with upstream Transformers :models:testing:
** TODO Test layer-level and full-forward FP32 parity :models:testing:
** TODO Test BF16 parity with explicit numerical tolerances :models:testing:
** TODO Test parameter gradients and objective gradients against upstream :models:testing:
** TODO Test one optimizer update against the upstream reference :models:testing:
** TODO Test safetensor export and upstream reload :models:testing:
** TODO Reduce and amortize ModernVBERT compilation cold-start cost :models:testing:performance:
** TODO Reduce ModernVBERT multimodal-forward allocator and process peak memory :models:testing:performance:
** TODO Revisit a maintained fused-attention kernel after profiling standard JAX :models:testing:performance:pallas:
** TODO Keep small golden tensors in ordinary CI :models:testing:
** TODO Run live upstream parity on a scheduled compatibility lane :models:testing:
** TODO Run accelerator-specific parity for fused kernels separately :models:testing:
** TODO Port a legally clean text-image encoder such as SigLIP2 :models:multimodal:
** TODO Select native audio and video architecture families :models:multimodal:

** TODO Add the host-side =embed= API for preprocessing and batching :api:
** TODO Keep compiled =encode= arrays-in and arrays-out :api:
** TODO Support reusable pooling, normalization, projection, and routing modules :api:
** TODO Reach Sentence Transformers 5.6.1 capability parity without copying its API :api:parity:
  Pin =sentence-transformers==5.6.1= as the repository-only oracle. Maintain a
  reviewed capability ledger rather than equating upstream class names with our
  public API. Dense bi-encoders, late-interaction/multi-vector encoders,
  cross-encoders, and sparse encoders are separate task and systems contracts;
  text, image, audio, video, and composed multimodal inputs cut across them.
  The system-level dense audit and the exact boundary we intend to implement
  live in =docs/dense-system-audit.md=.
*** TODO Complete the dense bi-encoder route :api:models:retrieval:
**** TODO Expand trainer input and integration capabilities
***** TODO Add default, no-duplicate, hashed no-duplicate, and label-group samplers
***** TODO Add proportional/round-robin multi-source and per-source task/loss routing
***** TODO Make prompts and typed routes first-class preprocessing configuration
***** TODO Add source-neutral offline hard-negative mining with artifact provenance
**** TODO Remove duplicate source-checkpoint reconstruction from native reload
     Reconstruct a typed, weightless template from bundled architecture metadata,
     then materialize exported native weights exactly once.
**** TODO Reduce cold train/evaluator compilation and steady-state update time
     Preserve model, task, precision, batch, and validation semantics; report
     persistent-cache behavior separately from an honest cold process.
*** TODO Implement late-interaction and multi-vector representation learning :tasks:late_interaction:
   Support token-level and learned multi-vector embeddings, ColBERT-style MaxSim,
   masking and compression, in-batch negatives, cached/replayed representations,
   retrieval evaluators, and efficient indexing. Treat the released 5.6.1 oracle
   as a baseline inventory, not as the ceiling for this track.
   Use PyLate as the implementation oracle, ColBERTv2 as the scientific oracle,
   and PLAID as the retrieval-system reference. Keep paper measurements in the
   paper campaign above.
** TODO Support =encode_query= and =encode_document= behavior through typed routes :api:retrieval:
** TODO Add Hugging Face Hub loading, local loading, and model export :api:hf:
** TODO Add multi-dataset functionality through source sampling policies :api:data:
** TODO Add unquantized and additional parameter-efficient adapter recipes :api:training:
   Reuse the general model-shaped trainable filter and export contract for
   ordinary LoRA and future useful adapter families; require model, optimizer,
   DDP/FSDP, resume, and export acceptance for each recipe.

** TODO Expand classification beyond paired softmax :tasks:classification:
  Add single-example and multi-label heads, class weighting, evaluator metrics,
  and source mappings without coupling them to the paired SoftmaxLoss contract.
** TODO Flesh out post-resolver data execution slices :data:planning:
  Define implementation contracts and acceptance gates for S3 and object
  stores, image/audio/video decoding and streaming, length- and modality-aware
  bucketing, scientifically valid packing, and deterministic distributed
  iterator resume. Keep JEST as the separate online selection method below.
** TODO Fingerprint artifact revisions, mapping code, recipes, and Git state :data:reproducibility:
** TODO Persist sampling policies instead of materialized mixture datasets :data:reproducibility:
** TODO Support static and scheduled mixture weights :data:sampling:
** TODO Support deterministic shuffling, sharding, and iterator resume :data:runtime:
** TODO Add length and modality-aware bucketing :data:performance:
** TODO Stream text examples from source through the model forward :data:text:
** TODO Stream image examples from source through the model forward :data:image:
** TODO Decode and stream audio efficiently through the model forward :data:audio:
** TODO Decode, sample, and stream video efficiently through the model forward :data:video:
** TODO Train mixed text, image, audio, video, and composed multimodal batches efficiently :data:multimodal:
** TODO Enforce a static model-ready ABI at the compiled step boundary :data:multimodal:runtime:
  Keep =train_step= as the canonical JIT boundary. Resolve variable records,
  decoding, tokenization, sampling, collation, and padding before it, and pass
  only clean tensors in one declared static signature or a small explicitly
  configured set. Compile each allowed signature deliberately before sustained
  training and reject unseen signatures rather than silently compiling inside a
  run. Measure padding waste per signature; do not regain compile stability by
  unconditionally materializing the largest multimodal bucket.

** TODO Optimize compiler-managed FSDP schedules across device counts :runtime:sharding:performance:
   Preserve exact shared-primitive annotations while improving the small-model
   PCIe schedule and eliminating avoidable AllToAlls. Accept only against matched
   numerical trajectories, capacity, and physical throughput; do not reintroduce
   a model-specific hook, separate trainer, custom VJP, or manual collective path.
** TODO Coalesce delayed FP8 scale state and re-run the physical frontier :runtime:precision:performance:
   Replace per-use dynamic weight reductions with persistent delayed scales and
   one coalesced update without changing optimizer or model semantics.  Accept
   only with bounded trajectory error, native optimized HLO, fewer FSDP
   collectives, and a matched end-to-end throughput win.  Evaluate block-scaled
   MXFP8/NVFP4 only on hardware that lowers those formats natively.
** TODO Overlap device placement with Grain input prefetching :runtime:performance:
  First prove from =data_wait_seconds=, placement timing, queue depth, and device
  traces that input or transfer bubbles reduce sustained training throughput.
  Only then build device-ready iterators with non-blocking =jax.device_put= on
  one host and =jax.make_array_from_process_local_data= for distributed input,
  and accept the change only when the observed bottleneck improves.
** TODO Precompile and cache the declared static step signatures :runtime:performance:
  Reuse the exact compiled =train_step= for every batch with the same model-ready
  ABI. Bound any finite signature set in configuration and never let raw record
  variability create an uncontrolled executable population.
* General development [0/18] [0%]

  This section tracks release engineering, optional integrations, exploratory
  research, deferred hardware validation, and work that is useful but is not a
  blocker for the first systems whitepaper.

** TODO Add compressed PLAID/WARP adapters behind the indexing protocol :tasks:late_interaction:
** WAITING Implement sparse encoder training and retrieval :tasks:sparse_encoder:
  Define native sparse representations, pooling, FLOPS regularization, task
  batches, cached losses, retrieval metrics, and one real checkpoint training
  gate. Sparse retrieval is intentionally outside the first paper.
** TODO Add sequence and multimodal packing where scientifically valid :data:performance:
  The paper diagnostic found that MPNet packing preserves embeddings, MNR loss,
  and gradients, but ordinary bucketing dominates compile-stable training
  packing. Revisit after the first paper only with length-aware batch formation
  plus a fixed ABI or an efficient ragged attention kernel, and integrate logical
  segment replay explicitly with GradCache. Evaluation-only packing remains the
  most promising first use. Preserve segment IDs, reset positions, cross-example
  attention boundaries, and task loss/example semantics.

** TODO Cut the next release after dense-system acceptance :release:
  Choose and set the next version only after the dense trainer, evaluator, and
  disk-to-final-model systems gates are complete. Tag only after the exact
  release-ready commit passes public CI and its wheel passes fresh CPU and CUDA
  13 smoke installs through the protected PyPI environment; do not reuse or
  replace the published 0.0.1 artifacts.
** TODO Define the stable public API and deprecation policy :release:api:
** TODO Evaluate Haliax for named axes, model composition, and sharding ergonomics :models:distributed:research:
** WAITING Evaluate TorchAX only as an optional compatibility backend :models:research:
** TODO Audit LCO licensing before considering a native port :models:release:
** TODO Resolve local WebDataset artifacts :data:local:
** TODO Resolve S3 and object-store artifacts first-class :data:s3:
** TODO Implement objective-general online joint batch selection from [[https://arxiv.org/abs/2406.17711][JEST]] :data:sampling:runtime:
  Select a training sub-batch from a larger super-batch using interactions
  exposed by the task objective, rather than scoring examples independently.
  Support easy-reference and learner-minus-reference learnability criteria,
  deterministic sequential chunk sampling without replacement, and arbitrary
  task-provided score matrices. Match the paper's reference algorithm exactly
  before adding cached reference scores, approximate scoring, asynchronous
  selection, distributed selection, or modality-specific accelerations.
** WAITING Validate fully distributed multi-host training :distributed:
  This milestone requires at least two physical accelerator hosts with a
  routable coordinator and is intentionally deferred until those resources are
  available.  Single-host replicated data parallelism and exact GradCache are
  already accepted and remain the local numerical oracle.
*** TODO Validate model and optimizer-state sharding across hosts
   Compare complete updates against the replicated oracle, measure collective
   and resharding costs, and verify that parameter, optimizer, and activation
   placement match the declared plan.
*** TODO Validate exact GradCache and global negatives across physical hosts
   Re-run the accepted process-local ModernVBERT contract with positives and
   negatives crossing the host boundary.  Preserve local encoder replay and
   exactly one final parameter-gradient synchronization.
*** TODO Validate multi-host checkpoint, restore, and failure behavior
   Restore into the same and different topologies, publish checkpoints without
   host races, and fail clearly on coordinator, worker, or storage loss.
*** TODO Record multi-host scaling and transport evidence
   Record topology, interconnect, compile time, steady-state throughput, peak
   memory, communication cost, and failure behavior before making a
   fully-distributed support claim.
** TODO Benchmark JAX =Ref= state against functional Equinox/Optax donation :runtime:performance:research:
  Keep the canonical functional update unless measured memory or throughput
  evidence shows that mutable =Ref= state from the JAX Training Cookbook is a
  monotonic improvement without weakening checkpoint or model ergonomics.
** TODO Evaluate Pallas for performance-critical fused kernels :runtime:performance:research:
** TODO Measure GPU and TPU behavior where hardware is available :research:benchmark:
** TODO Add and validate TPU accelerator telemetry :runtime:hardware:tpu:
  Implement the =logging.accelerator= backend for TPU, verify its available
  utilization and memory signals on real hardware, and preserve the existing
  metric names where the device semantics match NVIDIA telemetry.

** TODO Study JEPA-guided representation refinement for generative language models :research:jepa:language_model:
  First characterize the representation geometry already present in pretrained
  causal language models through JEPA-style invariance, prediction, transfer,
  and collapse diagnostics over grouped or labeled text sequences.  Then test a
  distinct representation-refinement phase that adds a JEPA objective before
  or alongside generative fine-tuning.  Measure whether it improves internal
  geometry, sample efficiency, controllability, and downstream generation while
  preserving language-model likelihood and generation quality; compare against
  continued pretraining, supervised fine-tuning, and contrastive controls.

** TODO Use the completed trainer to train a state-of-the-art representation model :research:model:
** TODO Write the subsequent model paper from that training program :research:paper:
