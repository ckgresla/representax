[project]
name = "representax-tpu-experiments"
version = "0.0.0"
requires-python = ">=3.13,<3.14"
dependencies = [
    "representax[hf,tpu]",
]

[tool.uv.sources]
representax = { path = "../..", editable = true }
